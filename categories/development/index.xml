<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>Development on Terra Nullius</title>
    <link>https://alexei-led.github.io/categories/development/</link>
    <description>Recent content in Development on Terra Nullius</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <copyright>This work is licensed under a Creative Commons Attribution 4.0 International License</copyright>
    <lastBuildDate>Mon, 23 Dec 2019 10:00:00 +0200</lastBuildDate>
    <atom:link href="https://alexei-led.github.io/categories/development/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>Kubernetes and Secrets Management in Cloud</title>
      <link>https://alexei-led.github.io/post/secrets-init/</link>
      <pubDate>Mon, 23 Dec 2019 10:00:00 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/secrets-init/</guid>
      <description>&lt;h2 id=&#34;introduction&#34;&gt;Introduction&lt;/h2&gt;
&lt;p&gt;Secrets are essential for operation of many production systems. Unintended secrets exposure is one of the top risks that should be properly addressed. Developers should do their best to protect application secrets.&lt;/p&gt;
&lt;p&gt;The problem becomes even harder, once company moves to a microservice architecture and multiple services require an access to different secrets in order to properly work. And this leads to a new challenges: how to distribute, manage, monitor and rotate application secrets, avoiding unintended exposure?&lt;/p&gt;
&lt;h2 id=&#34;kubernetes-secrets&#34;&gt;Kubernetes Secrets&lt;/h2&gt;
&lt;p&gt;Kubernetes provides an object called Secret, which you can use to store application sensitive data, like passwords, SSH keys, API keys, tokens and others. Kubernetes Secret can be injected into a Pod container either as an environment variable or mounted as a file. Using Kubernetes Secrets, allows to abstract sensitive data and configuration from application deployment.&lt;/p&gt;
&lt;p&gt;For example, a Kubernetes secret can be created with command &lt;code&gt;kubectl create secret&lt;/code&gt;:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;kubectl create secret generic db-credentials -n default --from-literal&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;user&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;dbuser --from-literal&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;password&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;quick.fox.5312
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;or Kubernetes &lt;code&gt;db-credentials.yaml&lt;/code&gt; file, that describes the same secret:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-yaml&#34; data-lang=&#34;yaml&#34;&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;apiVersion&lt;/span&gt;: v1
&lt;span style=&#34;color:#66d9ef&#34;&gt;kind&lt;/span&gt;: Secret
&lt;span style=&#34;color:#66d9ef&#34;&gt;metadata&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: db-credentials
&lt;span style=&#34;color:#66d9ef&#34;&gt;type&lt;/span&gt;: Opaque
&lt;span style=&#34;color:#66d9ef&#34;&gt;data&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;password&lt;/span&gt;: cXVpY2suZm94LjUzMTI=
  &lt;span style=&#34;color:#66d9ef&#34;&gt;user&lt;/span&gt;: ZGJ1c2Vy
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;kubectl create -f db-credentials.yaml
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Please note, that storing a sensitive data in a Kubernetes Secret does not make it secure. By default, all data in Kubernetes Secrets is stored as a plantext encoded with &lt;code&gt;base64&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Starting with version 1.13, Kubernetes supports &lt;a href=&#34;https://kubernetes.io/docs/tasks/administer-cluster/encrypt-data/&#34;&gt;encrypting Secrets data at rest&lt;/a&gt;, using &lt;code&gt;EncryptionConfiguration&lt;/code&gt; object with built-in or external encryption provider.&lt;/p&gt;
&lt;p&gt;List of currently supported encryption providers:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://kubernetes.io/docs/tasks/administer-cluster/encrypt-data/#providers&#34;&gt;built-in providers&lt;/a&gt;: &lt;code&gt;aescbc&lt;/code&gt;, &lt;code&gt;aesgsm&lt;/code&gt;, &lt;code&gt;secretbox&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://kubernetes.io/docs/tasks/administer-cluster/kms-provider/&#34;&gt;KMS providers&lt;/a&gt;:
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://cloud.google.com/kubernetes-engine/docs/how-to/encrypting-secrets&#34;&gt;Google KMS provider&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/kubernetes-sigs/aws-encryption-provider&#34;&gt;AWS KMS provider&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/Azure/kubernetes-kms&#34;&gt;Azure KMS provider&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;However Secrets encryption at rest is not enforced by default. And even when enabled, it is not sufficient and cannot be considered a complete secrets management solution.&lt;/p&gt;
&lt;p&gt;A complete secrets management solution must also support: secrets distribution, rotation, fine-grained access control, audit log, usage monitoring, versioning, strongly encrypted storage, convenient API and client SDK/s and probably some other useful features.&lt;/p&gt;
&lt;h2 id=&#34;cloud-secrets-management&#34;&gt;Cloud Secrets Management&lt;/h2&gt;
&lt;p&gt;Multiple cloud vendors provide secret management services, as part of their cloud platform offering, helping you to protect secrets needed to access applications, services, and APIs. Using these services eliminates the need to hardcode sensitive information in plain text and develop home-grown secrets management lifecycle. Secrets management services enable you to control access to application secrets using fine-grained permissions and auditing.&lt;/p&gt;
&lt;h2 id=&#34;integrating-kubernetes-with-secrets-management-services&#34;&gt;Integrating Kubernetes with Secrets Management services&lt;/h2&gt;
&lt;p&gt;In general, any application can use vendor-specific SDK/API to access secrets stored in a secrets management service. Usually this requires modifying application code or using some kind of bootstrap scripts or &lt;a href=&#34;https://kubernetes.io/docs/concepts/workloads/pods/init-containers/&#34;&gt;Init containers&lt;/a&gt;, using client CLI tools or web APIs.&lt;/p&gt;
&lt;h3 id=&#34;make-it-easy-with-secrets-init-tool&#34;&gt;Make it easy with &lt;code&gt;secrets-init&lt;/code&gt; tool&lt;/h3&gt;
&lt;p&gt;We release a &lt;a href=&#34;https://github.com/doitintl/secrets-init&#34;&gt;doitintl/secrets-init&lt;/a&gt; open source tool (Apache License 2.0) that simplifies integration of cloud-native Secrets Management services with containerized workload running on cloud-managed or self-managed Kubernetes clusters.&lt;/p&gt;
&lt;p&gt;In its essence, the &lt;code&gt;secrets-init&lt;/code&gt; is a minimalistic init system, designed to run as &lt;code&gt;PID 1&lt;/code&gt; inside a container environment, similarly to &lt;a href=&#34;https://github.com/Yelp/dumb-init&#34;&gt;dumb-init&lt;/a&gt;, and provides a fluent integration with multiple cloud-native Secrets Management services, like:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://aws.amazon.com/secrets-manager/&#34;&gt;AWS Secrets Manager&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://docs.aws.amazon.com/systems-manager/latest/userguide/systems-manager-parameter-store.html&#34;&gt;AWS Systems Manager Parameter Store&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://cloud.google.com/secret-manager/docs/&#34;&gt;Google Secret Manager (Beta)&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;why-you-need-an-init-system-in-docker-container&#34;&gt;Why you need an init system in Docker container?&lt;/h3&gt;
&lt;p&gt;Please &lt;a href=&#34;https://github.com/Yelp/dumb-init/blob/v1.2.0/README.md#why-you-need-an-init-system&#34;&gt;read Yelp &lt;em&gt;dumb-init&lt;/em&gt; repo explanation&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Summary:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Proper signal forwarding&lt;/li&gt;
&lt;li&gt;Orphaned zombies reaping&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;what-secrets-init-does&#34;&gt;What &lt;code&gt;secrets-init&lt;/code&gt; does&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;secrets-init&lt;/code&gt; runs as &lt;code&gt;PID 1&lt;/code&gt;, acting like a simple init system serving as &lt;code&gt;ENTRYPOINT&lt;/code&gt; or first container command, which is responsible to launch a child process, proxying all systems signals to a session rooted at that child process. This is the essence of init process. On the other hand, the &lt;code&gt;secrets-init&lt;/code&gt; also passes &lt;em&gt;almost&lt;/em&gt; all environment variables without modification, replacing special &lt;em&gt;secret variables&lt;/em&gt; with values from Secret Management services.&lt;/p&gt;
&lt;h3 id=&#34;integration-with-docker&#34;&gt;Integration with Docker&lt;/h3&gt;
&lt;p&gt;The &lt;code&gt;secrets-init&lt;/code&gt; is a statically compiled binary file (without external dependencies) and can be easily embedded included into any Docker image. Download &lt;code&gt;secrets-init&lt;/code&gt; binary and use it as the Docker container &lt;code&gt;ENTRYPOINT&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;For example:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-dockerfile&#34; data-lang=&#34;dockerfile&#34;&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;FROM&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt; node:alpine&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# download secrets-init binary&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;ENV&lt;/span&gt; SECRETS_INIT_VERSION&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;v0.2.1&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;ENV&lt;/span&gt; SECRETS_INIT_URL&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;https://github.com/doitintl/secrets-init/releases/download/v0.2.1/secrets-init_Linux_amd64.tar.gz&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;ENV&lt;/span&gt; SECRETS_INIT_SHA256&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;a2849460c650e9e7a29d9d0764e2b5fc679961e6667ad1c4416210fa791be29f
&lt;span style=&#34;color:#66d9ef&#34;&gt;RUN&lt;/span&gt; mkdir -p /opt/secrets-init &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; cd /opt/secrets-init &lt;span style=&#34;color:#ae81ff&#34;&gt;\
&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; wget -qO secrets-init.tar.gz &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt;$SECRETS_INIT_URL&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt; &lt;span style=&#34;color:#ae81ff&#34;&gt;\
&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; echo &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt;$SECRETS_INIT_SHA256&lt;span style=&#34;color:#e6db74&#34;&gt;  secrets-init.tar.gz&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt; | sha256sum -c - &lt;span style=&#34;color:#ae81ff&#34;&gt;\
&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; tar -xzvf secrets-init.tar.gz &lt;span style=&#34;color:#ae81ff&#34;&gt;\
&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; mv secrets-init /usr/local/bin &lt;span style=&#34;color:#ae81ff&#34;&gt;\
&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; rm secrets-init.tar.gz&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# set container entrypoint to secrets-init&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;ENTRYPOINT&lt;/span&gt; [&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;/usr/local/bin/secrets-init&amp;#34;&lt;/span&gt;]&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# ... copy app dependencies and code&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# ... define app CMD&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;h3 id=&#34;integration-with-kubernetes&#34;&gt;Integration with Kubernetes&lt;/h3&gt;
&lt;p&gt;In order to use &lt;code&gt;secrets-init&lt;/code&gt; with Kubernetes object (Pod/Deployment/Job/etc) without modifying Docker image, consider injecting &lt;code&gt;secrets-init&lt;/code&gt; into a target Pod through &lt;a href=&#34;https://kubernetes.io/docs/concepts/workloads/pods/init-containers/&#34;&gt;Init Container&lt;/a&gt;.
You can use the &lt;a href=&#34;https://hub.docker.com/r/doitintl/secrets-init&#34;&gt;doitint/secrets-init&lt;/a&gt; Docker image or create your own. Copy &lt;code&gt;secrets-init&lt;/code&gt; binary from init container to a common shared volume and change Pod &lt;code&gt;command&lt;/code&gt; to run &lt;code&gt;secrets-init&lt;/code&gt; as a first command.&lt;/p&gt;
&lt;h3 id=&#34;integration-with-aws-secrets-manager&#34;&gt;Integration with AWS Secrets Manager&lt;/h3&gt;
&lt;p&gt;To start using &lt;code&gt;secrets-init&lt;/code&gt; with AWS Secrets Manager, user should put an AWS secret ARN as environment variable value. The &lt;code&gt;secrets-init&lt;/code&gt; will resolve any environment value, using specified ARN, to a referenced secret value.&lt;/p&gt;
&lt;p&gt;Example, using &lt;code&gt;secrets-init&lt;/code&gt; with Kubernetes Job:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-yaml&#34; data-lang=&#34;yaml&#34;&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;apiVersion&lt;/span&gt;: batch/v1
&lt;span style=&#34;color:#66d9ef&#34;&gt;kind&lt;/span&gt;: Job
&lt;span style=&#34;color:#66d9ef&#34;&gt;metadata&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: printenv-job
&lt;span style=&#34;color:#66d9ef&#34;&gt;spec&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;template&lt;/span&gt;:
    &lt;span style=&#34;color:#66d9ef&#34;&gt;spec&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;restartPolicy&lt;/span&gt;: Never
      &lt;span style=&#34;color:#66d9ef&#34;&gt;serviceAccountName&lt;/span&gt;: iam-secrets-manager-ro
      &lt;span style=&#34;color:#66d9ef&#34;&gt;initContainers&lt;/span&gt;:
        - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: secrets-init
          &lt;span style=&#34;color:#66d9ef&#34;&gt;image&lt;/span&gt;: doitintl/secrets-init:v0&lt;span style=&#34;color:#ae81ff&#34;&gt;.2&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;.1&lt;/span&gt;
          &lt;span style=&#34;color:#66d9ef&#34;&gt;command&lt;/span&gt;:
            - sh
          &lt;span style=&#34;color:#66d9ef&#34;&gt;args&lt;/span&gt;:
            - -c
            - &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;cp /usr/local/bin/secrets-init /secrets-init/bin/&amp;#34;&lt;/span&gt;
          &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeMounts&lt;/span&gt;:
          - &lt;span style=&#34;color:#66d9ef&#34;&gt;mountPath&lt;/span&gt;: /secrets-init/bin
            &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: secrets-init-volume
      &lt;span style=&#34;color:#66d9ef&#34;&gt;containers&lt;/span&gt;:
      - &lt;span style=&#34;color:#66d9ef&#34;&gt;image&lt;/span&gt;: alpine:&lt;span style=&#34;color:#ae81ff&#34;&gt;3&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: print-env
        &lt;span style=&#34;color:#66d9ef&#34;&gt;env&lt;/span&gt;:
          - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: AWS_REGION
            &lt;span style=&#34;color:#66d9ef&#34;&gt;value&lt;/span&gt;: us-west&lt;span style=&#34;color:#ae81ff&#34;&gt;-2&lt;/span&gt;
          - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: TOP_SECRET
            &lt;span style=&#34;color:#66d9ef&#34;&gt;value&lt;/span&gt;: arn:aws:secretsmanager:us-west&lt;span style=&#34;color:#ae81ff&#34;&gt;-2&lt;/span&gt;:&lt;span style=&#34;color:#ae81ff&#34;&gt;906364353610&lt;/span&gt;:secret:topsecret-Acdaq8
        &lt;span style=&#34;color:#66d9ef&#34;&gt;command&lt;/span&gt;:
          - /secrets-init/bin/secrets-init
        &lt;span style=&#34;color:#66d9ef&#34;&gt;args&lt;/span&gt;:
          - sh
          - -c
          - &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;echo $TOP_SECRET&amp;#39;&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeMounts&lt;/span&gt;:
        - &lt;span style=&#34;color:#66d9ef&#34;&gt;mountPath&lt;/span&gt;: /secrets-init/bin
          &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: secrets-init-volume
      &lt;span style=&#34;color:#66d9ef&#34;&gt;volumes&lt;/span&gt;:
      - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: secrets-init-volume
        &lt;span style=&#34;color:#66d9ef&#34;&gt;emptyDir&lt;/span&gt;: {}
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;h3 id=&#34;integration-with-aws-systems-manager-parameter-store&#34;&gt;Integration with AWS Systems Manager Parameter Store&lt;/h3&gt;
&lt;p&gt;It is possible to use AWS Systems Manager Parameter Store to store application parameters as plain text or encrypted (kind of secrets).&lt;/p&gt;
&lt;p&gt;As with the previous example, user can put AWS Parameter Store ARN as environment variable value. The &lt;code&gt;secrets-init&lt;/code&gt; will resolve any environment value, using specified ARN, to a referenced parameter value.&lt;/p&gt;
&lt;p&gt;AWS Systems Manager Parameter Store format example:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# environment variable referencing AWS Systems Manager Parameter Store secret&lt;/span&gt;
MY_API_KEY&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;arn:aws:ssm:$AWS_REGION:$AWS_ACCOUNT_ID:parameter/api/key

&lt;span style=&#34;color:#75715e&#34;&gt;# environment variable passed to a child process, as resolved by `secrets-init`&lt;/span&gt;
MY_API_KEY&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;key-123456789
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;In order to resolve AWS secrets from AWS Secrets Manager and Parameter Store, &lt;code&gt;secrets-init&lt;/code&gt; should run under IAM role that has permission to access desired secrets.&lt;/p&gt;
&lt;p&gt;This can be achieved by assigning IAM Role to Kubernetes Pod or ECS Task. See &lt;a href=&#34;https://aws.amazon.com/blogs/opensource/introducing-fine-grained-iam-roles-service-accounts/&#34;&gt;Introducing fine-grained IAM roles for service accounts&lt;/a&gt; EKS blog post.&lt;/p&gt;
&lt;p&gt;It&amp;rsquo;s possible to assign IAM Role to EC2 instance, where container is running, but this option is considerably less secure and not recommended.&lt;/p&gt;
&lt;h3 id=&#34;integration-with-google-secret-manager&#34;&gt;Integration with Google Secret Manager&lt;/h3&gt;
&lt;p&gt;Google Cloud released recently a new service for managing secrets in the cloud: &lt;a href=&#34;https://cloud.google.com/secret-manager&#34;&gt;Google Secret Manager&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;User can put Google secret name, using &lt;code&gt;secrets-init&lt;/code&gt; recognizable prefix (&lt;code&gt;gcp:secretmanager:&lt;/code&gt;), following secret name (&lt;code&gt;projects/{PROJECT_ID}/secrets/{SECRET_NAME}&lt;/code&gt; or &lt;code&gt;projects/{PROJECT_ID}/secrets/{SECRET_NAME}/versions/{VERSION}&lt;/code&gt;) as environment variable value. The &lt;code&gt;secrets-init&lt;/code&gt; will resolve any environment value, using the specified name, to a referenced secret value.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# environment variable referencing Google Secret Manager secret (without version)&lt;/span&gt;
MY_DB_PASSWORD&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;gcp:secretmanager:projects/$PROJECT_ID/secrets/mydbpassword
&lt;span style=&#34;color:#75715e&#34;&gt;# OR versioned secret (with numeric version or &amp;#39;latest&amp;#39;)&lt;/span&gt;
MY_DB_PASSWORD&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;gcp:secretmanager:projects/$PROJECT_ID/secrets/mydbpassword/versions/2

&lt;span style=&#34;color:#75715e&#34;&gt;# environment variable passed to a child process, as resolved by `secrets-init`&lt;/span&gt;
MY_DB_PASSWORD&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;very-secret-password
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;In order to resolve Google secrets from Google Secret Manager, &lt;code&gt;secrets-init&lt;/code&gt; should run under IAM role that has permission to access desired secrets.&lt;/p&gt;
&lt;p&gt;This can be achieved by assigning IAM Role to Kubernetes Pod with &lt;a href=&#34;https://cloud.google.com/kubernetes-engine/docs/how-to/workload-identity&#34;&gt;Workload Identity&lt;/a&gt;. It&amp;rsquo;s possible to assign IAM Role to GCE instance, where container is running, but this option is less secure.&lt;/p&gt;
&lt;h2 id=&#34;summary&#34;&gt;Summary&lt;/h2&gt;
&lt;p&gt;Hope, you find this post useful. I look forward to your comments and any questions you have.&lt;/p&gt;
&lt;hr&gt;
&lt;p&gt;&lt;em&gt;This is a &lt;strong&gt;working draft&lt;/strong&gt; version. The final post version is published at &lt;a href=&#34;https://blog.doit-intl.com/&#34;&gt;DoiT Blog&lt;/a&gt; .&lt;/em&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>EKS GPU Cluster from Zero to Hero</title>
      <link>https://alexei-led.github.io/post/eks_gpu_spot/</link>
      <pubDate>Sat, 20 Jul 2019 10:00:00 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/eks_gpu_spot/</guid>
      <description>&lt;h2 id=&#34;introduction&#34;&gt;Introduction&lt;/h2&gt;
&lt;p&gt;If you ever tried to run a GPU workload on Kubernetes cluster, you know that this task requires non-trivial configuration and comes with high cost tag (GPU instances are quite expensive).&lt;/p&gt;
&lt;p&gt;This post shows how to run a GPU workload on Kubernetes cluster in cost effective way, using &lt;a href=&#34;https://aws.amazon.com/eks/&#34;&gt;AWS EKS&lt;/a&gt; cluster, &lt;a href=&#34;https://aws.amazon.com/autoscaling/&#34;&gt;AWS Auto Scaling&lt;/a&gt;, &lt;a href=&#34;https://aws.amazon.com/ec2/spot/&#34;&gt;Amazon EC2 Spot Instances&lt;/a&gt; and some Kubernetes resources and configurations.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://alexei-led.github.io/img/k8s_gpu.png&#34; alt=&#34;Kubernetes with GPU Mixed ASG&#34;&gt;&lt;/p&gt;
&lt;h2 id=&#34;eks-cluster-plan&#34;&gt;EKS Cluster Plan&lt;/h2&gt;
&lt;p&gt;First we need to create a Kubernetes cluster that consists from mixed nodes, non-GPU nodes for management and generic Kubernetes workload and more expensive GPU nodes to run GPU intensive tasks, like machine learning, medical analysis, seismic exploration, video transcoding and others.&lt;/p&gt;
&lt;p&gt;These node groups should be able to scale on demand (scale out and scale in) for generic nodes, and from 0 to required number and back to 0 for expensive GPU instances. More than that, in order to do it in cost effective way, we are going to use  &lt;a href=&#34;https://aws.amazon.com/ec2/spot/&#34;&gt;Amazon EC2 Spot Instances&lt;/a&gt; both for generic nodes and GPU nodes.&lt;/p&gt;
&lt;h3 id=&#34;aws-ec2-spot-instances&#34;&gt;AWS EC2 Spot Instances&lt;/h3&gt;
&lt;p&gt;With &lt;a href=&#34;https://aws.amazon.com/ec2/spot/&#34;&gt;Amazon EC2 Spot Instances&lt;/a&gt; instances you can save up to 90% comparing to On-Demand price. Previously, Spot instances were terminated in ascending order of bids. The market prices fluctuated frequently because of this. In the current model, the Spot prices are more predictable, updated less frequently, and are determined by Amazon EC2 spare capacity, not bid prices. AWS EC2 service can reclaim SPot instances when there is not enough capacity for specific instance in specific Availability Zone. Spot instances receive a 2 minute alert when are about to be reclaimed by Amazon EC2 service and can use this time for graceful shutdown and state change.&lt;/p&gt;
&lt;h2 id=&#34;the-workflow&#34;&gt;The Workflow&lt;/h2&gt;
&lt;h3 id=&#34;create-eks-cluster&#34;&gt;Create EKS Cluster&lt;/h3&gt;
&lt;p&gt;It is possible to create &lt;a href=&#34;https://aws.amazon.com/eks/&#34;&gt;AWS EKS&lt;/a&gt; cluster, using &lt;a href=&#34;https://docs.aws.amazon.com/cli/latest/reference/eks&#34;&gt;AWS EKS CLI&lt;/a&gt;, CloudFormation or Terraform, &lt;a href=&#34;https://aws.amazon.com/cdk/&#34;&gt;AWS CDK&lt;/a&gt; or &lt;a href=&#34;https://eksctl.io&#34;&gt;eksctl&lt;/a&gt;.&lt;/p&gt;
&lt;h4 id=&#34;eksctl-cli-tool&#34;&gt;eksctl CLI tool&lt;/h4&gt;
&lt;p&gt;In this post &lt;code&gt;eksctl&lt;/code&gt; (a CLI tool for creating clusters on EKS) is used.
It is possible to pass all parameters to the tool as CLI flags or configuration file. Using configuration file makes process more repeatable and automation friendly.&lt;/p&gt;
&lt;p&gt;&lt;code&gt;eksctl&lt;/code&gt; can create or update EKS cluster and additional required AWS resources, using CloudFormation stacks.&lt;/p&gt;
&lt;p&gt;Customize your cluster by using a config file. Just run&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;eksctl create cluster -f cluster.yaml
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;to apply a cluster.yaml file:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-yaml&#34; data-lang=&#34;yaml&#34;&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;apiVersion&lt;/span&gt;: eksctl.io/v1alpha5
&lt;span style=&#34;color:#66d9ef&#34;&gt;kind&lt;/span&gt;: ClusterConfig

&lt;span style=&#34;color:#66d9ef&#34;&gt;metadata&lt;/span&gt;:
&lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: test-cluster
&lt;span style=&#34;color:#66d9ef&#34;&gt;region&lt;/span&gt;: us-west&lt;span style=&#34;color:#ae81ff&#34;&gt;-2&lt;/span&gt;

&lt;span style=&#34;color:#66d9ef&#34;&gt;nodeGroups&lt;/span&gt;:
  - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: ng
    &lt;span style=&#34;color:#66d9ef&#34;&gt;instanceType&lt;/span&gt;: m5.large
    &lt;span style=&#34;color:#66d9ef&#34;&gt;desiredCapacity&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;10&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;A new EKS cluster with 10 &lt;code&gt;m5.large&lt;/code&gt; On-Demand EC2 worker nodes will be created and cluster credentials will be added to &lt;code&gt;~/.kube/config&lt;/code&gt; file.&lt;/p&gt;
&lt;h3 id=&#34;creating-node-groups&#34;&gt;Creating node groups&lt;/h3&gt;
&lt;p&gt;As planned, we are going to create two node groups for Kubernetes worker nodes:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;em&gt;General&lt;/em&gt; node group - autoscaling group with Spot instances to run Kubernetes system workload and non-GPU workload&lt;/li&gt;
&lt;li&gt;&lt;em&gt;GPU&lt;/em&gt; node groups - autoscaling group with GPU-powered Spot Instances, that can scale from 0 to required number of instances and back to 0.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Fortunately, the &lt;code&gt;eksctl&lt;/code&gt; supports adding Kubernetes node groups to EKS cluster and these groups can be composed from  Spot-only instances or mixture of Spot and On-Demand instances.&lt;/p&gt;
&lt;h4 id=&#34;general-node-group&#34;&gt;General node group&lt;/h4&gt;
&lt;p&gt;The &lt;code&gt;eksctl&lt;/code&gt; configuration file contains EKS cluster in &lt;code&gt;us-west-2&lt;/code&gt; across 3 Availability Zones and the first &lt;em&gt;General&lt;/em&gt; autoscaling (from 2 to 20 nodes) node group running on diversified Spot instances.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-yaml&#34; data-lang=&#34;yaml&#34;&gt;---
&lt;span style=&#34;color:#66d9ef&#34;&gt;apiVersion&lt;/span&gt;: eksctl.io/v1alpha5
&lt;span style=&#34;color:#66d9ef&#34;&gt;kind&lt;/span&gt;: ClusterConfig

&lt;span style=&#34;color:#66d9ef&#34;&gt;metadata&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: gaia-kube
  &lt;span style=&#34;color:#66d9ef&#34;&gt;region&lt;/span&gt;: us-west&lt;span style=&#34;color:#ae81ff&#34;&gt;-2&lt;/span&gt;

&lt;span style=&#34;color:#66d9ef&#34;&gt;availabilityZones&lt;/span&gt;: [&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;us-west-2a&amp;#34;&lt;/span&gt;, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;us-west-2b&amp;#34;&lt;/span&gt;, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;us-west-2c&amp;#34;&lt;/span&gt;]

&lt;span style=&#34;color:#66d9ef&#34;&gt;nodeGroups&lt;/span&gt;:
  &lt;span style=&#34;color:#75715e&#34;&gt;# spot workers NG - multi AZ, scale from 3&lt;/span&gt;
  - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: spot-ng
    &lt;span style=&#34;color:#66d9ef&#34;&gt;ami&lt;/span&gt;: auto
    &lt;span style=&#34;color:#66d9ef&#34;&gt;instanceType&lt;/span&gt;: mixed
    &lt;span style=&#34;color:#66d9ef&#34;&gt;desiredCapacity&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;2&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;minSize&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;2&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;maxSize&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;20&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeSize&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;100&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeType&lt;/span&gt;: gp2
    &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeEncrypted&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;iam&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;attachPolicyARNs&lt;/span&gt;:
        - arn:aws:iam::aws:policy/service-role/AmazonEC2RoleforSSM
        - arn:aws:iam::aws:policy/AmazonEKS_CNI_Policy
        - arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy
        - arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly
      &lt;span style=&#34;color:#66d9ef&#34;&gt;withAddonPolicies&lt;/span&gt;:
        &lt;span style=&#34;color:#66d9ef&#34;&gt;autoScaler&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;ebs&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;albIngress&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;cloudWatch&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;instancesDistribution&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;onDemandPercentageAboveBaseCapacity&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;0&lt;/span&gt;
      &lt;span style=&#34;color:#66d9ef&#34;&gt;instanceTypes&lt;/span&gt;:
        - m4.2xlarge
        - m4.4xlarge
        - m5.2xlarge
        - m5.4xlarge
        - m5a.2xlarge
        - m5a.4xlarge
        - c4.2xlarge
        - c4.4xlarge
        - c5.2xlarge
        - c5.4xlarge
      &lt;span style=&#34;color:#66d9ef&#34;&gt;spotInstancePools&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;15&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;tags&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;k8s.io/cluster-autoscaler/enabled&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;true&amp;#39;&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;labels&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;lifecycle&lt;/span&gt;: Ec2Spot
    &lt;span style=&#34;color:#66d9ef&#34;&gt;privateNetworking&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;availabilityZones&lt;/span&gt;: [&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;us-west-2a&amp;#34;&lt;/span&gt;, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;us-west-2b&amp;#34;&lt;/span&gt;, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;us-west-2c&amp;#34;&lt;/span&gt;]

    &lt;span style=&#34;color:#75715e&#34;&gt;### next: GPU node groups ...&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Now it is time to explain some parameters used in the above configuration file.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ami: auto&lt;/code&gt; - &lt;code&gt;eksctl&lt;/code&gt; automatically discover latest EKS-Optimized AMI image for worker nodes, based on specified AWS region, EKS version and instance type. See &lt;a href=&#34;https://docs.aws.amazon.com/eks/latest/userguide/eks-optimized-ami.html&#34;&gt;Amazon EKS-Optimized AMI&lt;/a&gt; chapter in User Guide&lt;/li&gt;
&lt;li&gt;&lt;code&gt;instanceType: mixed&lt;/code&gt; - specify that actual instance type will be one of instance types defined in &lt;code&gt;instancesDistribution&lt;/code&gt; section&lt;/li&gt;
&lt;li&gt;&lt;code&gt;iam&lt;/code&gt; contains list of predefined and in-place IAM policies; &lt;code&gt;eksctl&lt;/code&gt; creates a new &lt;a href=&#34;https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/iam-roles-for-amazon-ec2.html&#34;&gt;IAM Role&lt;/a&gt; with specified policies and attaches this role to every EKS worker node. There are several IAM policies you are required to attach to every EKS worker node, read &lt;a href=&#34;https://docs.aws.amazon.com/eks/latest/userguide/worker_node_IAM_role.html&#34;&gt;Amazon EKS Worker Node IAM Role&lt;/a&gt; section in User Guide and &lt;a href=&#34;https://eksctl.io/usage/iam-policies/&#34;&gt;&lt;code&gt;eksctl&lt;/code&gt; IAM policies&lt;/a&gt; documentation&lt;/li&gt;
&lt;li&gt;&lt;code&gt;instancesDistribution&lt;/code&gt; - specify mixed instance policy for EC2 Auto Scaling Groups, read AWS &lt;a href=&#34;https://docs.aws.amazon.com/autoscaling/ec2/APIReference/API_MixedInstancesPolicy.html&#34;&gt;MixedInstancesPolicy&lt;/a&gt; documentation&lt;/li&gt;
&lt;li&gt;&lt;code&gt;spotInstancePools&lt;/code&gt; - specifies number of Spot instance pools to use, &lt;a href=&#34;#Spot-Instance-Pools&#34;&gt;read more&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tags&lt;/code&gt; - AWS tags added to EKS worker nodes
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;k8s.io/cluster-autoscaler/enabled&lt;/code&gt; will use this tag for Kubernetes Cluster Autoscaler auto-discovery&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;code&gt;privateNetworking: true&lt;/code&gt; - all EKS worker nodes will be placed into private subnets&lt;/li&gt;
&lt;/ul&gt;
&lt;h5 id=&#34;spot-instance-pools&#34;&gt;Spot Instance Pools&lt;/h5&gt;
&lt;p&gt;When you are using Spot instances as worker nodes you need to diversify usage to as many &lt;em&gt;Spot Instance pools&lt;/em&gt; as possible. A &lt;em&gt;Spot Instance pool&lt;/em&gt; is a set of unused EC2 instances with the same instance type (for example, &lt;code&gt;m5.large&lt;/code&gt;), operating system, Availability Zone, and network platforms.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;eksctl&lt;/code&gt; currently supports single Spot provisioning model: &lt;code&gt;lowestPrice&lt;/code&gt; allocation strategy. This strategy allows creation of a fleet of Spot Instances that is both cheap and diversified. Spot Fleet automatically deploys the cheapest combination of instance types and Availability Zones based on the current Spot price across the number of Spot pools that you specify. This combination allows avoiding the most expensive Spot Instances.&lt;/p&gt;
&lt;p&gt;The Spot instance diversification also increases worker nodes availability, typically not all &lt;em&gt;Spot Instance pools&lt;/em&gt; will be interrupted at the same time, so only a small portion of your workload will be interrupted and EC2 Auto-scaling group will replace interrupted instances from others &lt;em&gt;Spot Instance pools&lt;/em&gt;.&lt;/p&gt;
&lt;h4 id=&#34;gpu-powered-node-group&#34;&gt;GPU-powered node group&lt;/h4&gt;
&lt;p&gt;The next part of our &lt;code&gt;eksctl&lt;/code&gt; configuration file contains first &lt;em&gt;GPU&lt;/em&gt; autoscaling (from 0 to 10 nodes) node group running on diversified GPU-powered Spot instances.&lt;/p&gt;
&lt;p&gt;When using GPU-powered Spot instances, it&amp;rsquo;s recommended to create &lt;em&gt;GPU&lt;/em&gt; node group per Availability Zone and configure Kubernetes Cluster Autoscaler to avoid automatic &lt;a href=&#34;https://docs.aws.amazon.com/autoscaling/ec2/userguide/auto-scaling-benefits.html#arch-AutoScalingMultiAZ&#34;&gt;ASG rebalancing&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;Why is it important?
GPU-powered EC2 Spot Instances have relatively high &lt;em&gt;Frequency of interruption&lt;/em&gt; rate (&lt;code&gt;&amp;gt;20%&lt;/code&gt; for some GPU instance types) and using multiple AZ and disabling automatic Cluster Autoscaler balancing can help to minimize GPU workload interruptions.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-yaml&#34; data-lang=&#34;yaml&#34;&gt;  &lt;span style=&#34;color:#75715e&#34;&gt;# ... EKS cluster and General node group ...&lt;/span&gt;

  &lt;span style=&#34;color:#75715e&#34;&gt;# spot GPU NG - west-2a AZ, scale from 0&lt;/span&gt;
  - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: gpu-spot-ng-a
    &lt;span style=&#34;color:#66d9ef&#34;&gt;ami&lt;/span&gt;: auto
    &lt;span style=&#34;color:#66d9ef&#34;&gt;instanceType&lt;/span&gt;: mixed
    &lt;span style=&#34;color:#66d9ef&#34;&gt;desiredCapacity&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;0&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;minSize&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;0&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;maxSize&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;10&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeSize&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;100&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeType&lt;/span&gt;: gp2
    &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeEncrypted&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;iam&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;attachPolicyARNs&lt;/span&gt;:
        - arn:aws:iam::aws:policy/service-role/AmazonEC2RoleforSSM
        - arn:aws:iam::aws:policy/AmazonEKS_CNI_Policy
        - arn:aws:iam::aws:policy/AmazonEKSWorkerNodePolicy
        - arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly
      &lt;span style=&#34;color:#66d9ef&#34;&gt;withAddonPolicies&lt;/span&gt;:
        &lt;span style=&#34;color:#66d9ef&#34;&gt;autoScaler&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;ebs&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;fsx&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;efs&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;albIngress&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;cloudWatch&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;instancesDistribution&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;onDemandPercentageAboveBaseCapacity&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;0&lt;/span&gt;
      &lt;span style=&#34;color:#66d9ef&#34;&gt;instanceTypes&lt;/span&gt;:
        - p3.2xlarge
        - p3.8xlarge
        - p3.16xlarge
        - p2.xlarge
        - p2.8xlarge
        - p2.16xlarge
      &lt;span style=&#34;color:#66d9ef&#34;&gt;spotInstancePools&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;tags&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;k8s.io/cluster-autoscaler/node-template/taint/dedicated&lt;/span&gt;: nvidia.com/gpu=&lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
      &lt;span style=&#34;color:#66d9ef&#34;&gt;k8s.io/cluster-autoscaler/node-template/label/nvidia.com/gpu&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;true&amp;#39;&lt;/span&gt;
      &lt;span style=&#34;color:#66d9ef&#34;&gt;k8s.io/cluster-autoscaler/enabled&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;true&amp;#39;&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;labels&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;lifecycle&lt;/span&gt;: Ec2Spot
      &lt;span style=&#34;color:#66d9ef&#34;&gt;nvidia.com/gpu&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;true&amp;#39;&lt;/span&gt;
      &lt;span style=&#34;color:#66d9ef&#34;&gt;k8s.amazonaws.com/accelerator&lt;/span&gt;: nvidia-tesla
    &lt;span style=&#34;color:#66d9ef&#34;&gt;taints&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;nvidia.com/gpu&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;true:NoSchedule&amp;#34;&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;privateNetworking&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;true&lt;/span&gt;
    &lt;span style=&#34;color:#66d9ef&#34;&gt;availabilityZones&lt;/span&gt;: [&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;us-west-2a&amp;#34;&lt;/span&gt;]

    &lt;span style=&#34;color:#75715e&#34;&gt;# create additional node groups for other `us-west-2b` and `us-west-2c` availability zones ...&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Now, it is time to explain some parameters used to configure GPU-powered node group.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;ami: auto&lt;/code&gt; - &lt;code&gt;eksctl&lt;/code&gt; automatically discover latest EKS-Optimized AMI image with GPU support for worker nodes, based on specified AWS region, EKS version and instance type. See &lt;a href=&#34;https://docs.aws.amazon.com/eks/latest/userguide/gpu-ami.html&#34;&gt;Amazon EKS-Optimized AMI with GPU support&lt;/a&gt; User Guide&lt;/li&gt;
&lt;li&gt;&lt;code&gt;iam: withAddonPolicies&lt;/code&gt; - if a planned workload requires access to AWS storage services, it is important to include additional IAM policies (auto-generated by &lt;code&gt;eksctl&lt;/code&gt;)
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;efs: true&lt;/code&gt; - enable access to &lt;a href=&#34;https://aws.amazon.com/efs/&#34;&gt;Amazon EFS&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;fsx: true&lt;/code&gt; - enable access to &lt;a href=&#34;https://aws.amazon.com/fsx/lustre/&#34;&gt;Amazon FSx for Lustre&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;code&gt;tags&lt;/code&gt; - AWS tags added to EKS worker nodes
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;k8s.io/cluster-autoscaler/node-template/taint/dedicated: nvidia.com/gpu=true&lt;/code&gt; - Kubernetes node taint&lt;/li&gt;
&lt;li&gt;&lt;code&gt;k8s.io/cluster-autoscaler/node-template/label/nvidia.com/gpu: &#39;true&#39;&lt;/code&gt; - Kubernetes node label used by Cluster Autoscaler to scale ASG from/to 0&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;&lt;code&gt;taints&lt;/code&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;nvidia.com/gpu: &amp;quot;true:NoSchedule&amp;quot;&lt;/code&gt; - Kubernetes GPU node taint; helps to avoid placement on non-GPU workload on expensive GPU nodes&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h5 id=&#34;eks-optimized-ami-image-with-gpu-support&#34;&gt;EKS Optimized AMI image with GPU support&lt;/h5&gt;
&lt;p&gt;In addition to the standard Amazon EKS-optimized AMI configuration, the GPU AMI includes the following:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;NVIDIA drivers&lt;/li&gt;
&lt;li&gt;The &lt;code&gt;nvidia-docker2&lt;/code&gt; package&lt;/li&gt;
&lt;li&gt;The &lt;code&gt;nvidia-container-runtime&lt;/code&gt; (as the default runtime)&lt;/li&gt;
&lt;/ul&gt;
&lt;h5 id=&#34;scaling-a-node-group-tofrom-0&#34;&gt;Scaling a node group to/from 0&lt;/h5&gt;
&lt;p&gt;From Kubernetes &lt;a href=&#34;https://github.com/kubernetes/autoscaler/&#34;&gt;Cluster Autoscaler&lt;/a&gt; 0.6.1 - it is possible to scale a node group to/from 0, assuming that all scale-up and scale-down conditions are met.&lt;/p&gt;
&lt;p&gt;If you are using &lt;code&gt;nodeSelector&lt;/code&gt; you need to tag the ASG with a node-template key &lt;code&gt;k8s.io/cluster-autoscaler/node-template/label/&lt;/code&gt; and &lt;code&gt;k8s.io/cluster-autoscaler/node-template/taint/&lt;/code&gt; if you are using taints.&lt;/p&gt;
&lt;h4 id=&#34;scheduling-gpu-workload&#34;&gt;Scheduling GPU workload&lt;/h4&gt;
&lt;h5 id=&#34;schedule-based-on-gpu-resources&#34;&gt;Schedule based on GPU resources&lt;/h5&gt;
&lt;p&gt;The &lt;a href=&#34;https://github.com/NVIDIA/k8s-device-plugin&#34;&gt;NVIDIA device plugin for Kubernetes&lt;/a&gt; exposes the number of GPUs on each nodes of your cluster. Once the plugin is installed, it&amp;rsquo;s possible to use &lt;code&gt;nvidia/gpu&lt;/code&gt; Kubernetes resource on GPU nodes and for Kubernetes workloads.&lt;/p&gt;
&lt;p&gt;Run this command to apply the Nvidia Kubernetes device plugin as a &lt;code&gt;daemonset&lt;/code&gt; running only on AWS GPU-powered worker nodes, using &lt;code&gt;tolerations&lt;/code&gt; and &lt;code&gt;nodeAffinity&lt;/code&gt;&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;kubectl create -f kubernetes/nvidia-device-plugin.yaml

kubectl get daemonset -nkube-system

NAME                                  DESIRED   CURRENT   READY   UP-TO-DATE   AVAILABLE   NODE SELECTOR   AGE
aws-node                              &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;         &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;         &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;       &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;            &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;           &amp;lt;none&amp;gt;          8d
kube-proxy                            &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;         &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;         &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;       &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;            &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;           &amp;lt;none&amp;gt;          8d
nvidia-device-plugin-daemonset-1.12   &lt;span style=&#34;color:#ae81ff&#34;&gt;3&lt;/span&gt;         &lt;span style=&#34;color:#ae81ff&#34;&gt;3&lt;/span&gt;         &lt;span style=&#34;color:#ae81ff&#34;&gt;3&lt;/span&gt;       &lt;span style=&#34;color:#ae81ff&#34;&gt;3&lt;/span&gt;            &lt;span style=&#34;color:#ae81ff&#34;&gt;3&lt;/span&gt;           &amp;lt;none&amp;gt;          8d
ssm-agent                             &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;         &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;         &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;       &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;            &lt;span style=&#34;color:#ae81ff&#34;&gt;5&lt;/span&gt;           &amp;lt;none&amp;gt;          8d
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;using &lt;code&gt;nvidia-device-plugin.yaml&lt;/code&gt; Kubernetes resource file&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-yaml&#34; data-lang=&#34;yaml&#34;&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;apiVersion&lt;/span&gt;: extensions/v1beta1
&lt;span style=&#34;color:#66d9ef&#34;&gt;kind&lt;/span&gt;: DaemonSet
&lt;span style=&#34;color:#66d9ef&#34;&gt;metadata&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: nvidia-device-plugin-daemonset&lt;span style=&#34;color:#ae81ff&#34;&gt;-1.12&lt;/span&gt;
  &lt;span style=&#34;color:#66d9ef&#34;&gt;namespace&lt;/span&gt;: kube-system
&lt;span style=&#34;color:#66d9ef&#34;&gt;spec&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;updateStrategy&lt;/span&gt;:
    &lt;span style=&#34;color:#66d9ef&#34;&gt;type&lt;/span&gt;: RollingUpdate
  &lt;span style=&#34;color:#66d9ef&#34;&gt;template&lt;/span&gt;:
    &lt;span style=&#34;color:#66d9ef&#34;&gt;metadata&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;labels&lt;/span&gt;:
        &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: nvidia-device-plugin-ds
    &lt;span style=&#34;color:#66d9ef&#34;&gt;spec&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;tolerations&lt;/span&gt;:
      - &lt;span style=&#34;color:#66d9ef&#34;&gt;key&lt;/span&gt;: nvidia.com/gpu
        &lt;span style=&#34;color:#66d9ef&#34;&gt;operator&lt;/span&gt;: Exists
        &lt;span style=&#34;color:#66d9ef&#34;&gt;effect&lt;/span&gt;: NoSchedule
      &lt;span style=&#34;color:#66d9ef&#34;&gt;containers&lt;/span&gt;:
      - &lt;span style=&#34;color:#66d9ef&#34;&gt;image&lt;/span&gt;: nvidia/k8s-device-plugin:&lt;span style=&#34;color:#ae81ff&#34;&gt;1.11&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: nvidia-device-plugin-ctr
        &lt;span style=&#34;color:#66d9ef&#34;&gt;securityContext&lt;/span&gt;:
          &lt;span style=&#34;color:#66d9ef&#34;&gt;allowPrivilegeEscalation&lt;/span&gt;: &lt;span style=&#34;color:#66d9ef&#34;&gt;false&lt;/span&gt;
          &lt;span style=&#34;color:#66d9ef&#34;&gt;capabilities&lt;/span&gt;:
            &lt;span style=&#34;color:#66d9ef&#34;&gt;drop&lt;/span&gt;: [&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;ALL&amp;#34;&lt;/span&gt;]
        &lt;span style=&#34;color:#66d9ef&#34;&gt;volumeMounts&lt;/span&gt;:
          - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: device-plugin
            &lt;span style=&#34;color:#66d9ef&#34;&gt;mountPath&lt;/span&gt;: /var/lib/kubelet/device-plugins
      &lt;span style=&#34;color:#66d9ef&#34;&gt;volumes&lt;/span&gt;:
        - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: device-plugin
          &lt;span style=&#34;color:#66d9ef&#34;&gt;hostPath&lt;/span&gt;:
            &lt;span style=&#34;color:#66d9ef&#34;&gt;path&lt;/span&gt;: /var/lib/kubelet/device-plugins
      &lt;span style=&#34;color:#66d9ef&#34;&gt;affinity&lt;/span&gt;:
        &lt;span style=&#34;color:#66d9ef&#34;&gt;nodeAffinity&lt;/span&gt;:
          &lt;span style=&#34;color:#66d9ef&#34;&gt;requiredDuringSchedulingIgnoredDuringExecution&lt;/span&gt;:
            &lt;span style=&#34;color:#66d9ef&#34;&gt;nodeSelectorTerms&lt;/span&gt;:
            - &lt;span style=&#34;color:#66d9ef&#34;&gt;matchExpressions&lt;/span&gt;:
              - &lt;span style=&#34;color:#66d9ef&#34;&gt;key&lt;/span&gt;: beta.kubernetes.io/instance-type
                &lt;span style=&#34;color:#66d9ef&#34;&gt;operator&lt;/span&gt;: In
                &lt;span style=&#34;color:#66d9ef&#34;&gt;values&lt;/span&gt;:
                - p3.2xlarge
                - p3.8xlarge
                - p3.16xlarge
                - p3dn.24xlarge
                - p2.xlarge
                - p2.8xlarge
                - p2.16xlarge
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;h5 id=&#34;taints-and-tolerations&#34;&gt;Taints and Tolerations&lt;/h5&gt;
&lt;p&gt;Kubernetes &lt;em&gt;taints&lt;/em&gt;  allow a node to repel a set of pods. Taints and tolerations work together to ensure that pods are not scheduled onto inappropriate nodes. One or more taints are applied to a node; this marks that the node should not accept any pods that do not tolerate the taints. Tolerations are applied to pods, and allow (but do not require) the pods to schedule onto nodes with matching taints.&lt;/p&gt;
&lt;p&gt;See Kubernetes &lt;a href=&#34;https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/&#34;&gt;Taints and Tolerations&lt;/a&gt; documentation for more details.&lt;/p&gt;
&lt;p&gt;In order to run GPU workload to run on GPU-powered Spot instance nodes, with &lt;code&gt;nvidia.com/gpu: &amp;quot;true:NoSchedule&amp;quot;&lt;/code&gt; taint, the workload must include both matching &lt;code&gt;tolerations&lt;/code&gt; and &lt;code&gt;nodeSelector&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;Kubernetes deployment with &lt;code&gt;10&lt;/code&gt; pod replicas with &lt;code&gt;nvidia/gpu: 1&lt;/code&gt; limit:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-yaml&#34; data-lang=&#34;yaml&#34;&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;apiVersion&lt;/span&gt;: apps/v1
&lt;span style=&#34;color:#66d9ef&#34;&gt;kind&lt;/span&gt;: Deployment
&lt;span style=&#34;color:#66d9ef&#34;&gt;metadata&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: cuda-vector-add
  &lt;span style=&#34;color:#66d9ef&#34;&gt;labels&lt;/span&gt;:
    &lt;span style=&#34;color:#66d9ef&#34;&gt;app&lt;/span&gt;: cuda-vector-add
&lt;span style=&#34;color:#66d9ef&#34;&gt;spec&lt;/span&gt;:
  &lt;span style=&#34;color:#66d9ef&#34;&gt;replicas&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;10&lt;/span&gt;
  &lt;span style=&#34;color:#66d9ef&#34;&gt;selector&lt;/span&gt;:
    &lt;span style=&#34;color:#66d9ef&#34;&gt;matchLabels&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;app&lt;/span&gt;: cuda-vector-add
  &lt;span style=&#34;color:#66d9ef&#34;&gt;template&lt;/span&gt;:
    &lt;span style=&#34;color:#66d9ef&#34;&gt;metadata&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: cuda-vector-add
      &lt;span style=&#34;color:#66d9ef&#34;&gt;labels&lt;/span&gt;:
        &lt;span style=&#34;color:#66d9ef&#34;&gt;app&lt;/span&gt;: cuda-vector-add
    &lt;span style=&#34;color:#66d9ef&#34;&gt;spec&lt;/span&gt;:
      &lt;span style=&#34;color:#66d9ef&#34;&gt;nodeSelector&lt;/span&gt;:
        &lt;span style=&#34;color:#66d9ef&#34;&gt;nvidia.com/gpu&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;true&amp;#34;&lt;/span&gt;
      &lt;span style=&#34;color:#66d9ef&#34;&gt;tolerations&lt;/span&gt;:
      - &lt;span style=&#34;color:#66d9ef&#34;&gt;key&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;nvidia.com/gpu&amp;#34;&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;operator&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;Exists&amp;#34;&lt;/span&gt;
        &lt;span style=&#34;color:#66d9ef&#34;&gt;effect&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;NoSchedule&amp;#34;&lt;/span&gt;
      &lt;span style=&#34;color:#66d9ef&#34;&gt;containers&lt;/span&gt;:
        - &lt;span style=&#34;color:#66d9ef&#34;&gt;name&lt;/span&gt;: cuda-vector-add
          &lt;span style=&#34;color:#75715e&#34;&gt;# https://github.com/kubernetes/kubernetes/blob/v1.7.11/test/images/nvidia-cuda/Dockerfile&lt;/span&gt;
          &lt;span style=&#34;color:#66d9ef&#34;&gt;image&lt;/span&gt;: &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;k8s.gcr.io/cuda-vector-add:v0.1&amp;#34;&lt;/span&gt;
          &lt;span style=&#34;color:#66d9ef&#34;&gt;resources&lt;/span&gt;:
            &lt;span style=&#34;color:#66d9ef&#34;&gt;limits&lt;/span&gt;:
              &lt;span style=&#34;color:#66d9ef&#34;&gt;nvidia.com/gpu&lt;/span&gt;: &lt;span style=&#34;color:#ae81ff&#34;&gt;1&lt;/span&gt; &lt;span style=&#34;color:#75715e&#34;&gt;# requesting 1 GPU&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Deploy &lt;code&gt;cuda-vector-add&lt;/code&gt; deployment and see how new GPU-powered nodes are added to the EKS cluster.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# list Kubernetes nodes before running GPU workload&lt;/span&gt;
NAME                                            ID                                      TYPE
ip-192-168-151-104.us-west-2.compute.internal   aws:///us-west-2b/i-01d4c83eaee18b7b3   c4.4xlarge
ip-192-168-171-140.us-west-2.compute.internal   aws:///us-west-2c/i-07ec09fd128e1393f   c4.4xlarge


&lt;span style=&#34;color:#75715e&#34;&gt;# deploy GPU workload on EKS cluster with tolerations for nvidia/gpu=true&lt;/span&gt;
kubectl create -f kubernetes/examples/vector/vector-add-dpl.yaml

&lt;span style=&#34;color:#75715e&#34;&gt;# list Kubernetes nodes after several minutes to see new GPU nodes added to the cluster&lt;/span&gt;
kubectl get nodes --output&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;custom-columns=NAME:.metadata.name,ID:.spec.providerID,TYPE:.metadata.labels.beta\.kubernetes\.io\/instance-type&amp;#34;&lt;/span&gt;

NAME                                            ID                                      TYPE
ip-192-168-101-60.us-west-2.compute.internal    aws:///us-west-2a/i-037d1994fe96eeffc   p2.16xlarge
ip-192-168-139-227.us-west-2.compute.internal   aws:///us-west-2b/i-0038eb8d2c795fb40   p2.16xlarge
ip-192-168-151-104.us-west-2.compute.internal   aws:///us-west-2b/i-01d4c83eaee18b7b3   c4.4xlarge
ip-192-168-171-140.us-west-2.compute.internal   aws:///us-west-2c/i-07ec09fd128e1393f   c4.4xlarge
ip-192-168-179-248.us-west-2.compute.internal   aws:///us-west-2c/i-0bc0853ef26c0c054   p2.16xlarge
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;As you can see, 3 new GPU-powered nodes (&lt;code&gt;p2.16xlarge&lt;/code&gt;), across 3 AZ, had been added to the cluster. When you delete GPU workload, the cluster will scale down GPU node group to 0 after 10 minutes.&lt;/p&gt;
&lt;h2 id=&#34;summary&#34;&gt;Summary&lt;/h2&gt;
&lt;p&gt;Follow this tutorial to create an EKS (Kubernetes) cluster with GPU-powered node group, running on Spot instances and scalable from/to 0 nodes.&lt;/p&gt;
&lt;h2 id=&#34;references&#34;&gt;References&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/alexei-led/eks-spot-cluster&#34;&gt;EKS Spot Cluster&lt;/a&gt; GitHub repository with code for this blog&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://itnext.io/the-definitive-guide-to-running-ec2-spot-instances-as-kubernetes-worker-nodes-68ef2095e767&#34;&gt;The definitive guide to running EC2 Spot Instances as Kubernetes worker nodes&lt;/a&gt; by Ran Sheinberg&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler&#34;&gt;Kubernetes Cluster Autoscaler&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href=&#34;https://kubernetes.io/docs/concepts/configuration/taint-and-toleration/&#34;&gt;Taints and Tolleratoins&lt;/a&gt; Kubernetes documentation&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;disclaimer&#34;&gt;Disclaimer&lt;/h2&gt;
&lt;p&gt;It does not matter where I work, all my opinions are my own.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Kubernetes Continuous Integration</title>
      <link>https://alexei-led.github.io/post/kubernetes_continuous_integration/</link>
      <pubDate>Fri, 08 Mar 2019 10:00:00 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/kubernetes_continuous_integration/</guid>
      <description>&lt;h1 id=&#34;kubernetes-configuration-as-code&#34;&gt;Kubernetes configuration as Code&lt;/h1&gt;
&lt;p&gt;Complex Kubernetes application consists from multiple Kubernetes resources, defined in YAML files. Authoring a properly formatted YAML files that are also a valid Kubernetes specification, that should also comply to some policy can be a challenging task.&lt;/p&gt;
&lt;p&gt;These YAML files are your application deployment and configuration code and should be addressed as code.&lt;/p&gt;
&lt;p&gt;As with code, Continuous Integration approach should be applied to a Kubernetes configuration files.&lt;/p&gt;
&lt;h2 id=&#34;git-repository&#34;&gt;Git Repository&lt;/h2&gt;
&lt;p&gt;Create a separate Git repository that contains Kubernetes configuration files. Define a Continuous Integration pipeline that is triggered automatically for for every change and can validate it without human intervention.&lt;/p&gt;
&lt;h2 id=&#34;helm&#34;&gt;Helm&lt;/h2&gt;
&lt;p&gt;Helm helps you manage complex Kubernetes applications. Using Helm Charts you define, install, test and upgrade Kubernetes application.&lt;/p&gt;
&lt;p&gt;Here I&amp;rsquo;m going to focus on using Helm for authoring complex Kubernetes application.&lt;/p&gt;
&lt;p&gt;The same Kubernetes application can be installed in multiple environments: development, testing, staging and production. Helm template helps to separate application structure from environment configuration by keeping environment specific values in external files.&lt;/p&gt;
&lt;h3 id=&#34;dependency-management&#34;&gt;Dependency Management&lt;/h3&gt;
&lt;p&gt;Helm also helps with dependency management. A typical Kubernetes application can be composed from multiple services developed by other teams and open source community.&lt;/p&gt;
&lt;p&gt;A &lt;code&gt;requirements.yaml&lt;/code&gt; file is a YAML file in which developers can declare Helm chart dependencies, along with the location of the chart and the desired version. For example, this requirements file declares two dependencies:&lt;/p&gt;
&lt;p&gt;Where possible, use version ranges instead of pinning to an exact version. The suggested default is to use a patch-level version match:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-txt&#34; data-lang=&#34;txt&#34;&gt;version: ~1.2.3
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;This will match version 1.2.3 and any patches to that release. In other words, ~1.2.3 is equivalent to &amp;gt;= 1.2.3, &amp;lt; 1.3.0&lt;/p&gt;
&lt;h2 id=&#34;yaml&#34;&gt;YAML&lt;/h2&gt;
&lt;p&gt;YAML is the most convenient way to write Kubernetes configuration files. YAML is easier for humans to read and write than other common data formats like XML or JSON. Still it&amp;rsquo;s recommended to use automatic YAML linters to avoid syntax and formatting errors.&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/adrienverge/yamllint&#34;&gt;yamlint&lt;/a&gt;&lt;/p&gt;
&lt;h3 id=&#34;helm-chart-validation&#34;&gt;Helm Chart Validation&lt;/h3&gt;
&lt;p&gt;Helm has a &lt;code&gt;helm lint&lt;/code&gt; command that runs a series of tests to verify that the chart is well-formed. The &lt;code&gt;helm lint&lt;/code&gt; also converts YAML to JSON, and this ways is able to detect some YAML errors.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;helm lint mychart

==&amp;gt; Linting mychart
[ERROR] templates/deployment.yaml: unable to parse YAML
    error converting YAML to JSON: yaml: line 53: did not find expected &amp;#39;-&amp;#39; indicator
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;There are few issues with &lt;code&gt;helm lint&lt;/code&gt;, you should be aware of:
- no real YAML validation is done: only YAML to JSON conversion errors are detected
- it shows wrong error line number: no the actual line in a template that contains the detected error&lt;/p&gt;
&lt;p&gt;So, I recommend also to use YAML linter, like &lt;code&gt;yamllint&lt;/code&gt; to perform YAML validation.&lt;/p&gt;
&lt;p&gt;First, you need to generate Kubernetes YAML files from a Helm chart. The &lt;code&gt;helm template&lt;/code&gt; renders chart templates locally and prints the output to the &lt;code&gt;stdout&lt;/code&gt;.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;helm template --namespace test --values dev-values.yaml
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Pipe &lt;code&gt;helm template&lt;/code&gt; and &lt;code&gt;yamllint&lt;/code&gt; together to validate rendered YAML files.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;helm template mychart | yamllint -

stdin
  41:81     error    line too long &lt;span style=&#34;color:#f92672&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;93&lt;/span&gt; &amp;gt; &lt;span style=&#34;color:#ae81ff&#34;&gt;80&lt;/span&gt; characters&lt;span style=&#34;color:#f92672&#34;&gt;)&lt;/span&gt;  &lt;span style=&#34;color:#f92672&#34;&gt;(&lt;/span&gt;line-length&lt;span style=&#34;color:#f92672&#34;&gt;)&lt;/span&gt;
  43:1      error    trailing spaces  &lt;span style=&#34;color:#f92672&#34;&gt;(&lt;/span&gt;trailing-spaces&lt;span style=&#34;color:#f92672&#34;&gt;)&lt;/span&gt;
  151:9     error    wrong indentation: expected &lt;span style=&#34;color:#ae81ff&#34;&gt;10&lt;/span&gt; but found &lt;span style=&#34;color:#ae81ff&#34;&gt;8&lt;/span&gt;  &lt;span style=&#34;color:#f92672&#34;&gt;(&lt;/span&gt;indentation&lt;span style=&#34;color:#f92672&#34;&gt;)&lt;/span&gt;
  245:10    error    syntax error: expected &amp;lt;block end&amp;gt;, but found &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;&amp;lt;block sequence start&amp;gt;&amp;#39;&lt;/span&gt;
  293:1     error    too many blank lines &lt;span style=&#34;color:#f92672&#34;&gt;(&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;1&lt;/span&gt; &amp;gt; 0&lt;span style=&#34;color:#f92672&#34;&gt;)&lt;/span&gt;  &lt;span style=&#34;color:#f92672&#34;&gt;(&lt;/span&gt;empty-lines&lt;span style=&#34;color:#f92672&#34;&gt;)&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Now there are multiple ways to inspect these errors:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# using vim editor&lt;/span&gt;
vim &amp;lt;&lt;span style=&#34;color:#f92672&#34;&gt;(&lt;/span&gt;helm template mychart&lt;span style=&#34;color:#f92672&#34;&gt;)&lt;/span&gt;

&lt;span style=&#34;color:#75715e&#34;&gt;# using cat command (with line number)&lt;/span&gt;
cat -n &amp;lt;&lt;span style=&#34;color:#f92672&#34;&gt;(&lt;/span&gt;helm template mychart&lt;span style=&#34;color:#f92672&#34;&gt;)&lt;/span&gt;

&lt;span style=&#34;color:#75715e&#34;&gt;# printing error line and few lines around it, replacing spaces with dots&lt;/span&gt;
helm template hermes | sed -n 240,250p | tr &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39; &amp;#39;&lt;/span&gt; &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#39;⋅&amp;#39;&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;h2 id=&#34;valid-kubernetes-configuration&#34;&gt;Valid Kubernetes Configuration&lt;/h2&gt;
&lt;p&gt;When authoring Kubernetes configuration files, it&amp;rsquo;s important not only check if they are valid YAML files, but if they are valid Kubernetes files.&lt;/p&gt;
&lt;p&gt;It turns out that the Kubernetes supports OpenAPI specification and it&amp;rsquo;s possible to generate Kubernetes JSON schema for every Kubernetes API version.&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/garethr&#34;&gt;Gareth Rushgrove&lt;/a&gt; wrote a &lt;a href=&#34;https://www.morethanseven.net/2017/06/26/schemas-for-kubernetes-types/&#34;&gt;blog post&lt;/a&gt; on this topic and maintains the &lt;a href=&#34;https://github.com/garethr/kubernetes-json-schema&#34;&gt;garethr/kubernetes-json-schema&lt;/a&gt; GitHub repository with most recent Kubernetes and OpenShift JSON schemas for all API versions. What a great contribution to the community!&lt;/p&gt;
&lt;p&gt;Now, with Kubernetes API JSON schema, it&amp;rsquo;s possible to validate any YAML file if it&amp;rsquo;s a valid Kubernetes configuration file.&lt;/p&gt;
&lt;p&gt;The &lt;a href=&#34;https://github.com/garethr/kubeval&#34;&gt;kubeval&lt;/a&gt; tool (also authored by Gareth Rushgrove) is to help.&lt;/p&gt;
&lt;p&gt;Run &lt;code&gt;kubeval&lt;/code&gt; piped with &lt;code&gt;helm template&lt;/code&gt; command.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;helm template mychart | kubeval
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;The output below sows single detected error in &lt;code&gt;Service&lt;/code&gt; definition: invalid annotation. The &lt;code&gt;kubeval&lt;/code&gt; could be more specific, by providing &lt;code&gt;Service&lt;/code&gt; name, but event AS IS it is a valuable output for detecting Kubernetes configuration errors.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-text&#34; data-lang=&#34;text&#34;&gt;The document stdin contains a valid Secret
The document stdin contains a valid Secret
The document stdin contains a valid ConfigMap
The document stdin contains a valid ConfigMap
The document stdin contains a valid PersistentVolumeClaim
The document stdin contains an invalid Service
---&amp;gt; metadata.annotations: Invalid type. Expected: object, given: null
The document stdin contains a valid Service
The document stdin contains a valid Deployment
The document stdin contains a valid Deployment
The document stdin is empty
The document stdin is empty
The document stdin is empty
The document stdin is empty
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;&lt;a href=&#34;https://github.com/garethr/kubetest&#34;&gt;https://github.com/garethr/kubetest&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Do not ignore .dockerignore</title>
      <link>https://alexei-led.github.io/post/donot_ignore_dockerignore/</link>
      <pubDate>Sat, 26 Nov 2016 16:00:00 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/donot_ignore_dockerignore/</guid>
      <description>&lt;h1 id=&#34;tldr&#34;&gt;TL;DR&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Tip:&lt;/strong&gt; Consider to define and use &lt;code&gt;.dockerignore&lt;/code&gt; file for every Docker image you are building. It can help you to reduce Docker image size, speedup &lt;code&gt;docker build&lt;/code&gt; and avoid unintended secret exposure.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;&lt;img src=&#34;https://alexei-led.github.io/img/overloaded.jpg&#34; alt=&#34;Overloaded container ship&#34;&gt;&lt;/p&gt;
&lt;h1 id=&#34;docker-build-context&#34;&gt;Docker build context&lt;/h1&gt;
&lt;p&gt;The &lt;code&gt;docker build&lt;/code&gt; command is used to build a new Docker image. There is one argument you can pass to the &lt;code&gt;build&lt;/code&gt; command &lt;strong&gt;build context&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;So, what is the Docker &lt;strong&gt;build context&lt;/strong&gt;?&lt;/p&gt;
&lt;p&gt;First, remember, that Docker is a client-server application, it consists from Docker client and Docker server (also known as &lt;em&gt;daemon&lt;/em&gt;). The Docker client command line tool talks with Docker server and asks it do things. One of these things is &lt;strong&gt;build&lt;/strong&gt;: building a new Docker image. The Docker server can run on the same machine as the client, remote machine or virtual machine, that also can be local, remote or even run on some cloud IaaS.&lt;/p&gt;
&lt;p&gt;Why is that important and how is the Docker &lt;strong&gt;build context&lt;/strong&gt; related to this fact?&lt;/p&gt;
&lt;p&gt;In order to create a new Docker image, Docker server needs an access to files, you want to create the Docker image from. So, you need somehow to send these files to the Docker server. These files are the Docker &lt;strong&gt;build context&lt;/strong&gt;. The Docker client packs all &lt;strong&gt;build context&lt;/strong&gt; files into &lt;code&gt;tar&lt;/code&gt; archive and uploads this archive to the Docker server. By default client will take all files (and folders) in current working directory and use them as the &lt;strong&gt;build context&lt;/strong&gt;.
It can also accept already created &lt;code&gt;tar&lt;/code&gt; archive or &lt;code&gt;git&lt;/code&gt; repository. In a case of &lt;code&gt;git&lt;/code&gt; repository, the client will clone it with submodules into a temporary folder and will create a &lt;strong&gt;build context&lt;/strong&gt; archive from it.&lt;/p&gt;
&lt;h1 id=&#34;impact-on-docker-build&#34;&gt;Impact on Docker build&lt;/h1&gt;
&lt;p&gt;The first output line, that you see, running the &lt;code&gt;docker build&lt;/code&gt; command is:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;Sending build context to Docker daemon 45.3 MB
Step 1: FROM ...
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;This should make things clear. Actually, &lt;strong&gt;every time&lt;/strong&gt; you are running the &lt;code&gt;docker build&lt;/code&gt; command, the Docker client creates a new &lt;strong&gt;build context&lt;/strong&gt; archive and sends it to the Docker server. So, you are always paying this &amp;ldquo;tax&amp;rdquo;: the time it takes to create an archive, storage and network traffic and latency time.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;strong&gt;Tip:&lt;/strong&gt; The &lt;strong&gt;rule of thumb&lt;/strong&gt; is not adding files to the &lt;strong&gt;build context&lt;/strong&gt;, if you do not need them in your Docker image.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h1 id=&#34;the-dockerignore-file&#34;&gt;The &lt;strong&gt;.dockerignore&lt;/strong&gt; file&lt;/h1&gt;
&lt;p&gt;The &lt;code&gt;.dockerignore&lt;/code&gt; file is the tool, that can help you to define the Docker &lt;strong&gt;build context&lt;/strong&gt; you really need. Using this file, you can specify &lt;strong&gt;ignore rules&lt;/strong&gt; and &lt;strong&gt;exceptions&lt;/strong&gt; from these rules for files and folder, that won&amp;rsquo;t be included in the &lt;strong&gt;build context&lt;/strong&gt; and thus won&amp;rsquo;t be packed into an archive and uploaded to the Docker server.&lt;/p&gt;
&lt;h1 id=&#34;why-should-you-care&#34;&gt;Why should you care?&lt;/h1&gt;
&lt;p&gt;Indeed, why should you care? Computers today are fast, networks are also pretty fast (hopefully) and storage is cheap. So, this &amp;ldquo;tax&amp;rdquo; may be not that big, right?
I will try to convince you, that you should care.&lt;/p&gt;
&lt;h2 id=&#34;reason-1-docker-image-size&#34;&gt;Reason #1: Docker image size&lt;/h2&gt;
&lt;p&gt;The world of software development is shifting lately towards &lt;em&gt;continuous delivery&lt;/em&gt;, &lt;em&gt;elastic infrastructure&lt;/em&gt; and &lt;em&gt;microservice architecture&lt;/em&gt;.&lt;/p&gt;
&lt;p&gt;How is that related?&lt;/p&gt;
&lt;p&gt;Your systems are composed of multiple components (or &lt;em&gt;microservices&lt;/em&gt;), each one of them running inside Linux container. There might be tens or hundreds of services and even more service instances. These service instances can be built and deployed independently of each other and this can be done for &lt;strong&gt;every single code commit&lt;/strong&gt;. More than that, &lt;em&gt;elastic infrastructure&lt;/em&gt; means that new compute nodes can be added or removed from the system and its microservices can move from node to node, to support scale or availability requirements. That means, your Docker images will be frequently built and transferred.&lt;/p&gt;
&lt;p&gt;When you practice continuous delivery and microservice architecture, image size and image build time &lt;strong&gt;do matter&lt;/strong&gt;.&lt;/p&gt;
&lt;h2 id=&#34;reason-2-unintended-secrets-exposure&#34;&gt;Reason #2: Unintended secrets exposure&lt;/h2&gt;
&lt;p&gt;Not controlling your &lt;strong&gt;build context&lt;/strong&gt;, can also lead to an unintended exposure of your code, commit history, and secrets (keys and credentials).&lt;/p&gt;
&lt;p&gt;If you copy files into you Docker image with &lt;code&gt;ADD .&lt;/code&gt; or &lt;code&gt;COPY .&lt;/code&gt; command, you may unintendedly include your source files, whole &lt;code&gt;git&lt;/code&gt; history (a &lt;code&gt;.git&lt;/code&gt; folder), secret files (like &lt;code&gt;.aws&lt;/code&gt;, &lt;code&gt;.env&lt;/code&gt;, private keys), cache and other files not only into the Docker &lt;strong&gt;build context&lt;/strong&gt;, but also into the final Docker image.&lt;/p&gt;
&lt;p&gt;There are multiple Docker images currently available on DockerHub, that expose application source code, passwords, keys and credentials (for example &lt;a href=&#34;http://thehackernews.com/2016/07/vine-source-code.html&#34;&gt;Twitter Vine&lt;/a&gt;).&lt;/p&gt;
&lt;h2 id=&#34;reason-3-the-docker-build---cache-invalidation&#34;&gt;Reason #3: The Docker build - cache invalidation&lt;/h2&gt;
&lt;p&gt;A common pattern is to inject an application&amp;rsquo;s entire codebase into an image using an instruction like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;COPY . /usr/src/app
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;In this case, we&amp;rsquo;re copying the &lt;strong&gt;entire&lt;/strong&gt; &lt;strong&gt;build context&lt;/strong&gt; into the image. It&amp;rsquo;s also important to understand, that every Dockerfile command generates a new layer. So, if any of included file changes in the entire build context, this change will invalidate the build cache for &lt;code&gt;COPY . /opt/myapp&lt;/code&gt; layer and a new image layer will be generated on the next build.&lt;/p&gt;
&lt;p&gt;If your working directory contains files that are frequently updated (logs, test results, git history, temporary cache files and similar), you are going to regenerate this layer for every &lt;code&gt;docker build&lt;/code&gt; run.&lt;/p&gt;
&lt;h1 id=&#34;the-dockerignore-syntax&#34;&gt;The &lt;code&gt;.dockerignore&lt;/code&gt; syntax&lt;/h1&gt;
&lt;p&gt;The &lt;code&gt;.dockerignore&lt;/code&gt; file is similar to &lt;code&gt;gitignore&lt;/code&gt; file, used by &lt;code&gt;git&lt;/code&gt; tool. similarly to &lt;code&gt;.gitignore&lt;/code&gt; file, it allows you to specify a pattern for files and folders that should be ignored by the Docker client when generating a &lt;strong&gt;build context&lt;/strong&gt;. While &lt;code&gt;.dockerignore&lt;/code&gt; file syntax used to describe &lt;strong&gt;ignore patterns&lt;/strong&gt; is similar to &lt;code&gt;.gitignore&lt;/code&gt; it&amp;rsquo;s not the same.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;.dockerignore&lt;/code&gt; pattern matching syntax is based on Go &lt;code&gt;filepath.Match()&lt;/code&gt; function and includes some additions.&lt;/p&gt;
&lt;p&gt;Here is the complete syntax for the &lt;code&gt;.dockerignore&lt;/code&gt;:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;pattern:
    { term }
term:
    &#39;*&#39;         matches any sequence of non-Separator characters
    &#39;?&#39;         matches any single non-Separator character
    &#39;[&#39; [ &#39;^&#39; ] { character-range } &#39;]&#39;
                character class (must be non-empty)
    c           matches character c (c != &#39;*&#39;, &#39;?&#39;, &#39;\\&#39;, &#39;[&#39;)
    &#39;\\&#39; c      matches character c

character-range:
    c           matches character c (c != &#39;\\&#39;, &#39;-&#39;, &#39;]&#39;)
    &#39;\\&#39; c      matches character c
    lo &#39;-&#39; hi   matches character c for lo &amp;lt;= c &amp;lt;= hi

additions:
  &#39;**&#39;        matches any number of directories (including zero)
  &#39;!&#39;         lines starting with ! (exclamation mark) can be used to make exceptions to exclusions
    &#39;#&#39;         lines starting with this character are ignored: use it for comments
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;strong&gt;Note:&lt;/strong&gt; Using the &lt;code&gt;!&lt;/code&gt; character is pretty tricky. The combination of it and patterns before and after line with the &lt;code&gt;!&lt;/code&gt; character can be used to create more advanced rules.&lt;/p&gt;
&lt;h2 id=&#34;examples&#34;&gt;Examples&lt;/h2&gt;
&lt;pre&gt;&lt;code&gt;# ignore .git and .cache folders
.git
.cache
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# ignore all *.class files in all folders, including build root
**/*.class
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# ignore all markdown files (md) beside all README*.md other than README-secret.md
*.md
!README*.md
README-secret.md
&lt;/code&gt;&lt;/pre&gt;&lt;h1 id=&#34;next&#34;&gt;Next&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;RTFM - &lt;a href=&#34;https://docs.docker.com/engine/reference/builder/#/dockerignore-file&#34;&gt;https://docs.docker.com/engine/reference/builder/#/dockerignore-file&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Use &lt;code&gt;.dockerignore&lt;/code&gt; in every project, where you are building Docker images&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;p&gt;Hope you find this post useful. I look forward to your comments and any questions you have.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Docker Swarm cluster with docker-in-docker on MacOS</title>
      <link>https://alexei-led.github.io/post/swarm_dind/</link>
      <pubDate>Thu, 06 Oct 2016 16:00:00 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/swarm_dind/</guid>
      <description>&lt;h2 id=&#34;tldr&#34;&gt;TL;DR&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;Docker-in-Docker &lt;a href=&#34;https://hub.docker.com/_/docker/&#34;&gt;dind&lt;/a&gt; can help you to run Docker Swarm cluster on your Macbook only with Docker for Mac (v1.12+). No &lt;code&gt;virtualbox&lt;/code&gt;, &lt;code&gt;docker-machine&lt;/code&gt;, &lt;code&gt;vagrant&lt;/code&gt; or other app is required.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&#34;the-beginning&#34;&gt;The Beginning&lt;/h2&gt;
&lt;p&gt;One day, I&amp;rsquo;ve decided to try running Docker 1.12 Swarm cluster on my MacBook Pro. Docker team did a great job releasing Docker for Mac, and from that time I forgot all problems I used to have with &lt;code&gt;boot2docker&lt;/code&gt;. I really like Docker for Mac: it’s fast, lightweight, tightly integrated with MacOS and significantly simplifies my life when working in changing network environment. The only missing thing is that it&amp;rsquo;s possible to create and work with single Docker daemon running inside &lt;code&gt;xhyve&lt;/code&gt; VM. Shit! I want a cluster.&lt;/p&gt;
&lt;p&gt;Of cause, it&amp;rsquo;s possible to create Swarm cluster with &lt;code&gt;docker-machine&lt;/code&gt; tool, but it&amp;rsquo;s not MacOS friendly and requires to install additional VM software, like VirtualBox or Parallels (why? I already have &lt;code&gt;xhyve&lt;/code&gt;!). I have different network for work office and home. At work I&amp;rsquo;m behind corporate proxy with multiple firewall filters. At home, of cause, life is better. &lt;code&gt;docker-machine&lt;/code&gt; requires to create dedicated VMs for each environment and thus force me juggling with different shell scripts when I switch from one to another. It&amp;rsquo;s possible, but it&amp;rsquo;s not fun.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;I just want to have multi-node Swarm cluster with Docker for Mac (and &lt;code&gt;xhyve&lt;/code&gt; VM). As simple as it is.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;I’m a lazy person and if there is an already existing solution, I will alway choose one, even if it’s not ideal. So, after googling for a while, I&amp;rsquo;ve failed to find any suitable solution or blog post. So, I’ve decided to create my own and share it with you.&lt;/p&gt;
&lt;h2 id=&#34;the-idea&#34;&gt;The Idea&lt;/h2&gt;
&lt;p&gt;The basic idea is to use Docker for Mac for running Swam master and several &lt;a href=&#34;https://hub.docker.com/_/docker/&#34;&gt;Docker-in-Docker containers&lt;/a&gt; for running Swarm worker nodes.&lt;/p&gt;
&lt;p&gt;First, lets init our Swarm master:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# init Swarm master
docker swarm init
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&amp;hellip; keep Swarm join token:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# get join token
SWARM_TOKEN=$(docker swarm join-token -q worker)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&amp;hellip; and Docker &lt;code&gt;xhyve&lt;/code&gt; VM IP:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# get Swarm master IP (Docker for Mac xhyve VM IP)
SWARM_MASTER=$(docker info | grep -w &#39;Node Address&#39; | awk &#39;{print $3}&#39;)
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&amp;hellip; now let&amp;rsquo;s create 3 worker nodes and join these nodes to our cluster&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# run NUM_WORKERS workers with SWARM_TOKEN
NUM_WORKERS=3
for i in $(seq “${NUM_WORKERS}&amp;quot;); do
  docker run -d --privileged --name worker-${i} --hostname=worker-${i} -p ${i}2375:2375 docker:1.12.1-dind
  docker --host=localhost:${i}2375 swarm join --token ${SWARM_TOKEN} ${SWARM_MASTER}:2377
done
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Listing all our Swarm cluster nodes:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# list Swarm nodes :)
docker node ls
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&amp;hellip; you should see something like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ID                           HOSTNAME  STATUS  AVAILABILITY  MANAGER STATUS
1f6z8pioh3vuaz84gyp0biqt0    worker-2  Ready   Active
35z72o6zjhs9u1h99lrwzvx5n    worker-3  Ready   Active
d9ph5cmc604wp1vhhs754nnxx *  moby      Ready   Active        Leader
dj3gnpv86uqrw4b9mo9ux4jb5    worker-1  Ready   Active
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;That&amp;rsquo;s all folks! Now, you have running Swarm cluster on your Macbook and your Docker client is talking with Swarm master.&lt;/p&gt;
&lt;h3 id=&#34;nice-tip&#34;&gt;Nice tip:&lt;/h3&gt;
&lt;p&gt;You can use very nice [Swarm visualizer] (&lt;a href=&#34;https://github.com/ManoMarks/docker-swarm-visualizer&#34;&gt;https://github.com/ManoMarks/docker-swarm-visualizer&lt;/a&gt;) by Mano Marks to see your Swarm cluster &amp;ldquo;in action&amp;rdquo;.&lt;/p&gt;
&lt;p&gt;Run it with following command:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;docker run -it -d -p 8000:8000 -e HOST=localhost -e PORT=8000 -v /var/run/docker.sock:/var/run/docker.sock manomarks/visualizer
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;And you should be able to see something like this (after you deploy some demo app):&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://alexei-led.github.io/img/docker_visualizer.png&#34; alt=&#34;Docker Swarm visualizer: Voting App&#34;&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Network emulation for Docker containers</title>
      <link>https://alexei-led.github.io/post/pumba_docker_netem/</link>
      <pubDate>Mon, 01 Aug 2016 20:00:00 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/pumba_docker_netem/</guid>
      <description>&lt;h2 id=&#34;tldr&#34;&gt;TL;DR&lt;/h2&gt;
&lt;blockquote&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/gaia-adm/pumba&#34;&gt;Pumba&lt;/a&gt; &lt;code&gt;netem delay&lt;/code&gt; and &lt;code&gt;netem loss&lt;/code&gt; commands can emulate network &lt;em&gt;delay&lt;/em&gt; and &lt;em&gt;packet loss&lt;/em&gt; between Docker containers, even on single host. Give it a try!&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&#34;introduction&#34;&gt;Introduction&lt;/h2&gt;
&lt;p&gt;Microservice architecture has been adopted by software teams as a way to deliver business value faster. Container technology enables delivery of microservices into any environment. Docker has accelerated this by providing an easy to use toolset for development teams to build, ship, and run distributed applications. These applications can be composed of hundreds of microservices packaged in Docker containers.&lt;/p&gt;
&lt;p&gt;In a recent NGINX &lt;a href=&#34;https://www.nginx.com/resources/library/app-dev-survey/&#34;&gt;survey&lt;/a&gt; [Finding #7], the “biggest challenge holding back developers” is the trade-off between quality and speed. As Martin Fowler indicates, &lt;a href=&#34;http://martinfowler.com/articles/microservice-testing&#34;&gt;testing strategies in microservices architecture&lt;/a&gt; can be very complex. Creating a realistic and useful testing environment is an aspect of this complexity.&lt;/p&gt;
&lt;p&gt;One challenge is &lt;strong&gt;simulating network failures&lt;/strong&gt; to ensure &lt;strong&gt;resiliency&lt;/strong&gt; of applications and services.&lt;/p&gt;
&lt;p&gt;The network is a critical arterial system for ensuring reliability for any distributed application. Network conditions are different depending on where the application is accessed. Network behavior can greatly impact the overall application availability, stability, performance, and user experience (UX). It’s critical to simulate and understand these impacts before the user notices. Testing for these conditions requires conducting realistic network tests.&lt;/p&gt;
&lt;p&gt;After Docker containers are deployed in a cluster, all communication between containers happen over the network. These containers run on a single host, different hosts, different networks, and in different datacenters.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;How can we test for the impact of network behavior on the application? What can we do to emulate different network properties between containers on a single host or among clusters on multiple hosts?&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id=&#34;pumba-with-network-emulation&#34;&gt;Pumba with Network Emulation&lt;/h2&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/gaia-adm/pumba&#34;&gt;Pumba&lt;/a&gt; is a chaos testing tool for Docker containers, inspired by &lt;a href=&#34;https://github.com/Netflix/SimianArmy/wiki/Chaos-Monkey&#34;&gt;Netflix Chaos Monkey&lt;/a&gt;. The main benefit is that it works with containers instead of VMs. Pumba can kill, stop, restart running Docker containers or pause processes within specified containers. We use it for resilience testing of our distributed applications. Resilience testing ensures reliability of the system. It allows the team to verify their application recovers correctly regardless of any event (expected or unexpected) without any loss of data or functionality. Pumba simulates these events for distributed and containerized applications.&lt;/p&gt;
&lt;h3 id=&#34;pumba-netem&#34;&gt;Pumba &lt;code&gt;netem&lt;/code&gt;&lt;/h3&gt;
&lt;p&gt;We enhanced &lt;a href=&#34;https://github.com/gaia-adm/pumba&#34;&gt;Pumba&lt;/a&gt; with network emulation capabilities starting with &lt;em&gt;delay&lt;/em&gt; and &lt;em&gt;packet loss&lt;/em&gt;. Using &lt;code&gt;pumba netem&lt;/code&gt; command we can apply &lt;em&gt;delay&lt;/em&gt; or &lt;em&gt;packet loss&lt;/em&gt; on any Docker container. Under the hood, &lt;strong&gt;Pumba&lt;/strong&gt; uses Linux kernel traffic control (&lt;a href=&#34;http://man7.org/linux/man-pages/man8/tc.8.html&#34;&gt;tc&lt;/a&gt;) with &lt;a href=&#34;http://man7.org/linux/man-pages/man8/tc-netem.8.html&#34;&gt;netem&lt;/a&gt; queueing discipline. To work, we need to add &lt;a href=&#34;https://wiki.linuxfoundation.org/networking/iproute2&#34;&gt;iproute2&lt;/a&gt; to Docker images, that we want to test. Some base Docker images already include &lt;a href=&#34;https://wiki.linuxfoundation.org/networking/iproute2&#34;&gt;iproute2&lt;/a&gt; package.&lt;/p&gt;
&lt;p&gt;Pumba &lt;code&gt;netem delay&lt;/code&gt; and &lt;code&gt;netem loss&lt;/code&gt; commands can emulate network &lt;em&gt;delay&lt;/em&gt; and &lt;em&gt;packet loss&lt;/em&gt; between Docker containers, even on a single host.&lt;/p&gt;
&lt;p&gt;Linux has a built-in network emulation capabilities, starting from kernel 2.6.7 (released 14 years ago). Linux allows us to manipulate traffic control settings, using &lt;a href=&#34;http://man7.org/linux/man-pages/man8/tc.8.html&#34;&gt;tc&lt;/a&gt; tool, available in &lt;a href=&#34;https://wiki.linuxfoundation.org/networking/iproute2&#34;&gt;iproute2&lt;/a&gt;; &lt;a href=&#34;http://man7.org/linux/man-pages/man8/tc-netem.8.html&#34;&gt;netem&lt;/a&gt; is an extension (&lt;em&gt;queueing discipline&lt;/em&gt;) of the tc tool. It allows emulation of network properties — &lt;em&gt;delay&lt;/em&gt;, &lt;em&gt;packet loss&lt;/em&gt;, &lt;em&gt;packer reorder&lt;/em&gt;, &lt;em&gt;duplication&lt;/em&gt;, &lt;em&gt;corruption&lt;/em&gt;, and &lt;em&gt;bandwidth rate&lt;/em&gt;.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Pumba&lt;/strong&gt; &lt;code&gt;netem&lt;/code&gt; commands can help development teams simulate realistic network conditions as they build, ship, and run microservices in Docker containers.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Pumba&lt;/strong&gt; with low level &lt;code&gt;netem&lt;/code&gt; options, greatly simplifies its usage. We have made it easier to emulate different network properties for running Docker containers.&lt;/p&gt;
&lt;p&gt;In the current release, &lt;strong&gt;Pumba&lt;/strong&gt; modifies &lt;em&gt;egress&lt;/em&gt; traffic only by adding &lt;em&gt;delay&lt;/em&gt; or &lt;em&gt;packet loss&lt;/em&gt; for specified container(s). Target containers can be specified by name (single name or as a space separated list) or via regular expression (&lt;a href=&#34;https://github.com/google/re2/wiki/Syntax&#34;&gt;RE2&lt;/a&gt;). &lt;strong&gt;Pumba&lt;/strong&gt; modifies container network conditions for a specified duration. After a set time interval, &lt;strong&gt;Pumba&lt;/strong&gt; restores normal network conditions. &lt;strong&gt;Pumba&lt;/strong&gt; also restores the original connection with a graceful shutdown of the &lt;code&gt;pumba&lt;/code&gt; process &lt;code&gt;Ctrl-C&lt;/code&gt; or by stopping the &lt;strong&gt;Pumba&lt;/strong&gt; container with &lt;code&gt;docker stop&lt;/code&gt; command.
An option is available to apply an IP range filter to the network emulation. With this option, &lt;strong&gt;Pumba&lt;/strong&gt; will modify outgoing traffic for specified IP and will leave other outgoing traffic unchanged. Using this option, we can change network properties for a specific inter-container connection(s) as well as specific Docker networks — each Docker network has its own IP range.&lt;/p&gt;
&lt;h3 id=&#34;pumba-delay-netem-delay&#34;&gt;Pumba delay: &lt;code&gt;netem delay&lt;/code&gt;&lt;/h3&gt;
&lt;p&gt;To demonstrate, we’ll run two Docker containers: one is running a &lt;code&gt;ping&lt;/code&gt; command and the other is &lt;strong&gt;Pumba&lt;/strong&gt; Docker container, that adds 3 seconds network &lt;em&gt;delay&lt;/em&gt; to the ping container for 1 minute. After 1 minute, &lt;strong&gt;Pumba&lt;/strong&gt; container restores the network connection properties of the ping container as it exits gracefully.&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://asciinema.org/a/82428?t=7&#34;&gt;&lt;!-- raw HTML omitted --&gt;&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# open two terminal windows: (1) and (2)

# terminal (1)
# create new &#39;tryme&#39; Alpine container (with iproute2) and ping `www.example.com`
$ docker run -it --rm --name tryme alpine sh -c &amp;quot;apk add --update iproute2 &amp;amp;&amp;amp; ping www.example.com&amp;quot;

# terminal (2)
# run pumba: add 3s delay to `tryme` container for 1m
$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
         pumba netem --interface eth0 --duration 1m delay --time 3000 tryme

# See `ping` delay increased by 3000ms for 1 minute
# You can stop Pumba earlier with `Ctrl-C`
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;netem-delay-examples&#34;&gt;&lt;code&gt;netem delay&lt;/code&gt; examples&lt;/h3&gt;
&lt;p&gt;This section contains more advanced network emulation examples for &lt;code&gt;delay&lt;/code&gt; command.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# add 3 seconds delay for all outgoing packets on device `eth0` (default) of `mydb` Docker container for 5 minutes

$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
    pumba netem --duration 5m \
      delay --time 3000 \
      mydb
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# add a delay of 3000ms ± 30ms, with the next random element depending 20% on the last one,
# for all outgoing packets on device `eth1` of all Docker container, with name start with `hp`
# for 10 minutes

$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
    pumba netem --duration 5m --interface eth1 \
      delay \
        --time 3000 \
        --jitter 30 \
        --correlation 20 \
      re2:^hp
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# add a delay of 3000ms ± 40ms, where variation in delay is described by `normal` distribution,
# for all outgoing packets on device `eth0` of randomly chosen Docker container from the list
# for 10 minutes

$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
    pumba --random \
      netem --duration 5m \
        delay \
          --time 3000 \
          --jitter 40 \
          --distribution normal \
        container1 container2 container3
&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;pumba-packet-loss-netem-loss-netem-loss-state-netem-loss-gemodel&#34;&gt;Pumba packet loss: &lt;code&gt;netem loss&lt;/code&gt;, &lt;code&gt;netem loss-state&lt;/code&gt;, &lt;code&gt;netem loss-gemodel&lt;/code&gt;&lt;/h2&gt;
&lt;p&gt;Lets start with &lt;em&gt;packet loss&lt;/em&gt; demo. Here we will run three Docker containers. &lt;code&gt;iperf&lt;/code&gt; &lt;strong&gt;server&lt;/strong&gt; and &lt;strong&gt;client&lt;/strong&gt; for sending data and Pumba Docker container, that will add packer loss on client container.
We are using &lt;strong&gt;perform network throughput tests&lt;/strong&gt; tool &lt;a href=&#34;http://manpages.ubuntu.com/manpages/xenial/man1/iperf.1.html&#34;&gt;iperf&lt;/a&gt; to demonstrate &lt;em&gt;packet loss&lt;/em&gt;.&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://asciinema.org/a/82430&#34;&gt;&lt;!-- raw HTML omitted --&gt;&lt;/a&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# open three terminal windows

# terminal (1) iperf server
# server: `-s` run in server mode; `-u` use UDP;  `-i 1` report every second
$ docker run -it --rm --name tryme-srv alpine sh -c &amp;quot;apk add --update iperf &amp;amp;&amp;amp; iperf -s -u -i 1&amp;quot;

# terminal (2) iperf client
# client: `-c` client connects to &amp;lt;server ip&amp;gt;; `-u` use UDP
$ docker run -it --rm --name tryme alpine sh -c &amp;quot;apk add --update iproute2 iperf &amp;amp;&amp;amp; iperf -c 172.17.0.3 -u&amp;quot;

# terminal (3)
# run pumba: add 20% packet loss to `tryme` container for 1m
$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
         pumba netem --duration 1m loss --percent 20 tryme

# See server report on terminal (1) &#39;Lost/Total Datagrams&#39; - should see lost packets there
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;It is generally understood that &lt;em&gt;packet loss&lt;/em&gt; distribution in IP networks is “bursty”. To simulate more realistic &lt;em&gt;packet loss&lt;/em&gt; events, different probability models are used.
Pumba currently supports 3 different loss probability models for *packet loss&amp;rdquo;. Pumba defines separate &lt;em&gt;loss&lt;/em&gt; command for each probability model.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;loss&lt;/code&gt; - independent probability loss model (Bernoulli model); it&amp;rsquo;s the most widely used loss model where packet losses are modeled by a random process consisting of Bernoulli trails&lt;/li&gt;
&lt;li&gt;&lt;code&gt;loss-state&lt;/code&gt; - 2-state, 3-state and 4-state State Markov models&lt;/li&gt;
&lt;li&gt;&lt;code&gt;loss-gemodel&lt;/code&gt; - Gilbert and Gilbert-Elliott models&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Papers on network packer loss models:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&amp;ldquo;Indepth: Packet Loss Burstiness&amp;rdquo; &lt;a href=&#34;http://www.voiptroubleshooter.com/indepth/burstloss.html&#34;&gt;link&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&amp;ldquo;Definition of a general and intuitive loss model for packet networks and its implementation in the Netem module in the Linux kernel.&amp;rdquo; &lt;a href=&#34;netgroup.uniroma2.it/TR/TR-loss-netem.pdf&#34;&gt;link&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;code&gt;man netem&lt;/code&gt; &lt;a href=&#34;http://man7.org/linux/man-pages/man8/tc-netem.8.html&#34;&gt;link&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;netem-loss-examples&#34;&gt;&lt;code&gt;netem loss&lt;/code&gt; examples&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;# loss 0.3% of packets
# apply for `eth0` network interface (default) of `mydb` Docker container for 5 minutes

$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
    pumba netem --duration 5m \
      loss --percent 0.3 \
      mydb
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# loss 1.4% of packets (14 packets from 1000 will be lost)
# each successive probability (of loss) depends by a quarter on the last one
#   Prob(n) = .25 * Prob(n-1) + .75 * Random
# apply on `eth1` network interface  of Docker containers (name start with `hp`) for 15 minutes

$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
    pumba netem --interface eth1 --duration 15m \
      loss --percent 1.4 --correlation 25 \
      re2:^hp
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# use 2-state Markov model for packet loss probability: P13=15%, P31=85%
# apply on `eth1` network interface of 3 Docker containers (c1, c2 and c3) for 12 minutes

$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
    pumba netem --interface eth1 --duration 12m \
      loss-state -p13 15 -p31 85 \
      c1 c2 c3
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# use Gilbert-Elliot model for packet loss probability: p=5%, r=90%, (1-h)=85%, (1-k)=7%
# apply on `eth2` network interface of `mydb` Docker container for 9 minutes and 30 seconds

$ docker run -it --rm -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba \
    pumba netem --interface eth2 --duration 9m30s \
      loss-gemodel --pg 5 --pb 90 --one-h 85 --one-k 7 \
      mydb
&lt;/code&gt;&lt;/pre&gt;&lt;h2 id=&#34;contribution&#34;&gt;Contribution&lt;/h2&gt;
&lt;p&gt;Special thanks to &lt;a href=&#34;https://medium.com/@GehaniNeil&#34;&gt;Neil Gehani&lt;/a&gt; for helping me with this post and to &lt;a href=&#34;https://github.com/inbarshani&#34;&gt;Inbar Shani&lt;/a&gt; for initial Pull Request with &lt;code&gt;netem&lt;/code&gt; command.&lt;/p&gt;
&lt;h2 id=&#34;next&#34;&gt;Next&lt;/h2&gt;
&lt;p&gt;To see more examples on how to use Pumba with [netem] commands, please refer to the &lt;a href=&#34;https://github.com/gaia-adm/pumba&#34;&gt;Pumba GitHub Repository&lt;/a&gt;. We have open sourced it. We gladly accept ideas, pull requests, issues, or any other contributions.&lt;/p&gt;
&lt;p&gt;Pumba can be downloaded as precompiled binary (Windows, Linux and MacOS) from the &lt;a href=&#34;https://github.com/gaia-adm/pumba/releases&#34;&gt;GitHub project release page&lt;/a&gt;. It’s also available as a &lt;a href=&#34;https://hub.docker.com/r/gaiaadm/pumba/&#34;&gt;Docker image&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/gaia-adm/pumba&#34;&gt;Pumba GitHub Repository&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Pumba - Chaos Testing for Docker</title>
      <link>https://alexei-led.github.io/post/pumba_docker_chaos_testing/</link>
      <pubDate>Sat, 16 Apr 2016 20:00:00 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/pumba_docker_chaos_testing/</guid>
      <description>&lt;p&gt;&lt;strong&gt;Update (27-07-27):&lt;/strong&gt; Updated post to latest &lt;code&gt;v0.2.0&lt;/code&gt; Pumba version change.&lt;/p&gt;
&lt;h2 id=&#34;introduction&#34;&gt;Introduction&lt;/h2&gt;
&lt;p&gt;The best defense against unexpected failures is to build resilient services. Testing for resiliency enables the teams to learn where their apps fail before the customer does. By intentionally causing failures as part of resiliency testing, you can enforce your policy for building resilient systems.
Resilience of the system can be defined as its ability to continue functioning even if some components of the system are failing - &lt;a href=&#34;https://en.wikipedia.org/wiki/Ephemerality&#34;&gt;ephemerality&lt;/a&gt;. Growing popularity of distributed and microservice architecture makes resilience testing critical for applications that now require 24x7x365 operation.
Resilience testing is an approach where you intentionally inject different types of failures at the infrastructure level (VM, network, containers, and processes) and let the system try to recover from these unexpected failures that can happen in production. Simulating realistic failures at any time is the best way to enforce highly available and resilient systems.&lt;/p&gt;
&lt;h2 id=&#34;what-is-pumba&#34;&gt;What is Pumba?&lt;/h2&gt;
&lt;p&gt;&lt;img src=&#34;https://alexei-led.github.io/img/pumba_docker.png&#34; alt=&#34;Pumba&#34;&gt;&lt;/p&gt;
&lt;p&gt;First of all, &lt;a href=&#34;https://en.wikipedia.org/wiki/Timon_and_Pumbaa&#34;&gt;Pumba&lt;/a&gt; (or Pumbaa) is a supporting character from Disney&amp;rsquo;s animated film &lt;em&gt;The Lion King&lt;/em&gt;. In Swahili, &lt;em&gt;pumbaa&lt;/em&gt; means &amp;ldquo;to be foolish, silly, weak-minded, careless, negligent&amp;rdquo;. This reflects the unexpected behavior of the application.&lt;/p&gt;
&lt;p&gt;Pumba is inspired by highly popular &lt;a href=&#34;https://github.com/Netflix/SimianArmy/wiki/Chaos-Monkey&#34;&gt;Netfix Chaos Monkey&lt;/a&gt; resilience testing tool for AWS cloud. Pumba takes a similar approach but applies it at the container level. It connects to the Docker daemon running on some machine (local or remote) and brings a level of chaos to it: &amp;ldquo;randomly&amp;rdquo; killing, stopping, and removing running containers.&lt;/p&gt;
&lt;p&gt;If your system is designed to be resilient, it should be able to recover from such failures. &amp;ldquo;Failed&amp;rdquo; services should be restarted and lost connections should be recovered. This is not as trivial as it sounds. You need to design your services differently. Be aware that a service can fail (for whatever reason) or service it depends on can disappear at any point of time (but can reappear later). Expect the unexpected!&lt;/p&gt;
&lt;h2 id=&#34;why-run-pumba&#34;&gt;Why run Pumba?&lt;/h2&gt;
&lt;p&gt;Failures happen and they inevitably happen when least desired. If your application cannot recover from system failures, you are going to face angry customers and maybe even loose them. If you want to be sure that your system is able to recover from unexpected failures, it would be better to take charge of them and inject failures yourself instead of waiting till they happen. This is not a one time effort. In age of Continuous Delivery, you need to be sure that every change to any one of system services, does not compromise system availability. That&amp;rsquo;s why you should practice &lt;strong&gt;continuous resilience testing&lt;/strong&gt;.
With Docker gaining popularity as people are deploying and running clusters of containers in production. Using a container orchestration network (e.g. Kubernetes, Swarm, CoreOS fleet), it&amp;rsquo;s possible to restart a &amp;ldquo;failed&amp;rdquo; container automatically. How can you be sure that restarted services and other system services can properly recover from failures? If you are not using container orchestration frameworks, life is even harder: you will need to handle container restarts by yourself.&lt;/p&gt;
&lt;p&gt;This is where Pumba shines. You can run it on every Docker host, in your cluster, and Pumba will &amp;ldquo;randomly&amp;rdquo; stop running containers - matching specified name/s or name patterns. You can even specify the &lt;em&gt;signal&lt;/em&gt; that will be sent to &amp;ldquo;kill&amp;rdquo; the container.&lt;/p&gt;
&lt;h2 id=&#34;what-pumba-can-do&#34;&gt;What Pumba can do?&lt;/h2&gt;
&lt;p&gt;Pumba can create different failures for your running Docker containers. Pumba can kill, stop or remove running containers. It can also pause all processes withing running container for specified period of time.
Pumba can also do network emulation, simulating different network failures, like: delay, packet loss/corruption/reorder, bandwidth limits and more.
&lt;em&gt;Disclaimer:&lt;/em&gt; &lt;code&gt;netem&lt;/code&gt; command is under development and only &lt;code&gt;delay&lt;/code&gt; command is supported in Pumba &lt;code&gt;v0.2.0&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;You can pass list of containers to Pumba or just write a regular expression to select matching containers. If you will not specify containers, Pumba will try to disturb all running containers. Use &lt;code&gt;--random&lt;/code&gt; option, to randomly select only one target container from provided list.&lt;/p&gt;
&lt;h2 id=&#34;how-to-run-pumba&#34;&gt;How to run Pumba?&lt;/h2&gt;
&lt;p&gt;There are two ways to run Pumba.&lt;/p&gt;
&lt;p&gt;First, you can download Pumba application (single binary file) for your OS from project &lt;a href=&#34;https://github.com/gaia-adm/pumba/releases&#34;&gt;release page&lt;/a&gt; and run &lt;code&gt;pumba help&lt;/code&gt; to see list of supported commands and options.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;$ pumba help

Pumba version v0.2.0
NAME:
   Pumba - Pumba is a resilience testing tool, that helps applications tolerate random Docker container failures: process, network and performance.

USAGE:
   pumba [global options] command [command options] containers (name, list of names, RE2 regex)

VERSION:
   v0.2.0

COMMANDS:
     kill     kill specified containers
     netem    emulate the properties of wide area networks
     pause    pause all processes
     stop     stop containers
     rm       remove containers
     help, h  Shows a list of commands or help for one command

GLOBAL OPTIONS:
   --host value, -H value      daemon socket to connect to (default: &amp;quot;unix:///var/run/docker.sock&amp;quot;) [$DOCKER_HOST]
   --tls                       use TLS; implied by --tlsverify
   --tlsverify                 use TLS and verify the remote [$DOCKER_TLS_VERIFY]
   --tlscacert value           trust certs signed only by this CA (default: &amp;quot;/etc/ssl/docker/ca.pem&amp;quot;)
   --tlscert value             client certificate for TLS authentication (default: &amp;quot;/etc/ssl/docker/cert.pem&amp;quot;)
   --tlskey value              client key for TLS authentication (default: &amp;quot;/etc/ssl/docker/key.pem&amp;quot;)
   --debug                     enable debug mode with verbose logging
   --json                      produce log in JSON format: Logstash and Splunk friendly
   --slackhook value           web hook url; send Pumba log events to Slack
   --slackchannel value        Slack channel (default #pumba) (default: &amp;quot;#pumba&amp;quot;)
   --interval value, -i value  recurrent interval for chaos command; use with optional unit suffix: &#39;ms/s/m/h&#39;
   --random, -r                randomly select single matching container from list of target containers
   --dry                       dry runl does not create chaos, only logs planned chaos commands
   --help, -h                  show help
   --version, -v               print the version
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;kill-container-command&#34;&gt;Kill Container command&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;$ pumba kill -h

NAME:
   pumba kill - kill specified containers

USAGE:
   pumba kill [command options] containers (name, list of names, RE2 regex)

DESCRIPTION:
   send termination signal to the main process inside target container(s)

OPTIONS:
   --signal value, -s value  termination signal, that will be sent by Pumba to the main process inside target container(s) (default: &amp;quot;SIGKILL&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;pause-container-command&#34;&gt;Pause Container command&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;$ pumba pause -h

NAME:
   pumba pause - pause all processes

USAGE:
   pumba pause [command options] containers (name, list of names, RE2 regex)

DESCRIPTION:
   pause all running processes within target containers

OPTIONS:
   --duration value, -d value  pause duration: should be smaller than recurrent interval; use with optional unit suffix: &#39;ms/s/m/h&#39;
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;stop-container-command&#34;&gt;Stop Container command&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;$ pumba stop -h
NAME:
   pumba stop - stop containers

USAGE:
   pumba stop [command options] containers (name, list of names, RE2 regex)

DESCRIPTION:
   stop the main process inside target containers, sending  SIGTERM, and then SIGKILL after a grace period

OPTIONS:
   --time value, -t value  seconds to wait for stop before killing container (default 10) (default: 10)
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;remove-rm-container-command&#34;&gt;Remove (rm) Container command&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;$ pumba rm -h

NAME:
   pumba rm - remove containers

USAGE:
   pumba rm [command options] containers (name, list of names, RE2 regex)

DESCRIPTION:
   remove target containers, with links and voluems

OPTIONS:
   --force, -f    force the removal of a running container (with SIGKILL)
   --links, -l    remove container links
   --volumes, -v  remove volumes associated with the container
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;network-emulation-netem-command&#34;&gt;Network Emulation (netem) command&lt;/h3&gt;
&lt;pre&gt;&lt;code&gt;$ pumba netem -h

NAME:
   Pumba netem - delay, loss, duplicate and re-order (run &#39;netem&#39;) packets, to emulate different network problems

USAGE:
   Pumba netem command [command options] [arguments...]

COMMANDS:
     delay      dealy egress traffic
     loss
     duplicate
     corrupt

OPTIONS:
   --duration value, -d value   network emulation duration; should be smaller than recurrent interval; use with optional unit suffix: &#39;ms/s/m/h&#39;
   --interface value, -i value  network interface to apply delay on (default: &amp;quot;eth0&amp;quot;)
   --target value, -t value     target IP filter; netem will impact only on traffic to target IP
   --help, -h                   show help

NAME:
   Pumba netem - delay, loss, duplicate and re-order (run &#39;netem&#39;) packets, to emulate different network problems

USAGE:
   Pumba netem command [command options] [arguments...]

COMMANDS:
     delay      dealy egress traffic
     loss       TODO: planned to implement ...
     duplicate  TODO: planned to implement ...
     corrupt    TODO: planned to implement ...

OPTIONS:
   --duration value, -d value   network emulation duration; should be smaller than recurrent interval; use with optional unit suffix: &#39;ms/s/m/h&#39;
   --interface value, -i value  network interface to apply delay on (default: &amp;quot;eth0&amp;quot;)
   --target value, -t value     target IP filter; netem will impact only on traffic to target IP
   --help, -h                   show help
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&#34;network-emulation-delay-sub-command&#34;&gt;Network Emulation Delay sub-command&lt;/h4&gt;
&lt;pre&gt;&lt;code&gt;$ pumba netem delay -h

NAME:
   Pumba netem delay - dealy egress traffic

USAGE:
   Pumba netem delay [command options] containers (name, list of names, RE2 regex)

DESCRIPTION:
   dealy egress traffic for specified containers; networks show variability so it is possible to add random variation; delay variation isn&#39;t purely random, so to emulate that there is a correlation

OPTIONS:
   --amount value, -a value       delay amount; in milliseconds (default: 100)
   --variation value, -v value    random delay variation; in milliseconds; example: 100ms ± 10ms (default: 10)
   --correlation value, -c value  delay correlation; in percents (default: 20)
&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&#34;examples&#34;&gt;Examples&lt;/h4&gt;
&lt;pre&gt;&lt;code&gt;# stop random container once in a 10 minutes
$ ./pumba --random --interval 10m kill --signal SIGSTOP
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# every 15 minutes kill `mysql` container and every hour remove containers starting with &amp;quot;hp&amp;quot;
$ ./pumba --interval 15m kill --signal SIGTERM mysql &amp;amp;
$ ./pumba --interval 1h rm re2:^hp &amp;amp;
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# every 30 seconds kill &amp;quot;worker1&amp;quot; and &amp;quot;worker2&amp;quot; containers and every 3 minutes stop &amp;quot;queue&amp;quot; container
$ ./pumba --interval 30s kill --signal SIGKILL worker1 worker2 &amp;amp;
$ ./pumba --interval 3m stop queue &amp;amp;
&lt;/code&gt;&lt;/pre&gt;&lt;pre&gt;&lt;code&gt;# Once in 5 minutes, Pumba will delay for 2 seconds (2000ms) egress traffic for some (randomly chosen) container,
# named `result...` (matching `^result` regexp) on `eth2` network interface.
# Pumba will restore normal connectivity after 2 minutes. Print debug trace to STDOUT too.
$ ./pumba --debug --interval 5m --random netem --duration 2m --interface eth2 delay --amount 2000 re2:^result
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;running-pumba-in-docker-container&#34;&gt;Running Pumba in Docker Container&lt;/h3&gt;
&lt;p&gt;The second approach to run it in a Docker container.&lt;/p&gt;
&lt;p&gt;In order to give Pumba access to Docker daemon on host machine, you will need to mount &lt;code&gt;var/run/docker.sock&lt;/code&gt; unix socket.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;# run latest stable Pumba docker image (from master repository)
$ docker run -d -v /var/run/docker.sock:/var/run/docker.sock gaiaadm/pumba:master pumba kill --interval 10s --signal SIGTERM ^hp
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Pumba will not kill its own container.&lt;/p&gt;
&lt;p&gt;Note: For Mac OSX - before you run Pumba, you may want to do the following after downloading the &lt;a href=&#34;https://github.com/gaia-adm/pumba/releases&#34;&gt;pumba_darwin_amd64&lt;/a&gt; binary:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;chmod +x pumba_darwin_amd64
mv pumba_darwin_amd64 /usr/local/bin/pumba
pumba
&lt;/code&gt;&lt;/pre&gt;&lt;h3 id=&#34;next&#34;&gt;Next&lt;/h3&gt;
&lt;p&gt;The Pumba project is available for you to try out. We will gladly accept ideas, pull requests, issues, and contributions to the project.&lt;/p&gt;
&lt;p&gt;&lt;a href=&#34;https://github.com/gaia-adm/pumba&#34;&gt;Pumba GitHub Repository&lt;/a&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Testing Strategies for Docker Containers</title>
      <link>https://alexei-led.github.io/post/docker_testing/</link>
      <pubDate>Mon, 07 Mar 2016 16:39:00 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/docker_testing/</guid>
      <description>&lt;p&gt;Congratulations! You know how to build a Docker image and are able to compose multiple containers into a meaningful application. Hopefully, you&amp;rsquo;ve already created a Continuous Delivery pipeline and know how to push your newly created image into production or testing environment.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Now, the question is - &lt;em&gt;How do we test our Docker containers?&lt;/em&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;There are multiple testing strategies we can apply. In this post, I&amp;rsquo;ll highlight them presenting benefits and drawbacks for each.&lt;/p&gt;
&lt;h2 id=&#34;the-naive-approach&#34;&gt;The &amp;ldquo;Naive&amp;rdquo; approach&lt;/h2&gt;
&lt;p&gt;This is the default approach for most people. It relies on a CI server to do the job. When taking this approach, the developer is using Docker as a package manager, a better option than the &lt;strong&gt;jar/rpm/deb&lt;/strong&gt; approach.
The CI server compiles the application code and executes tests (unit, service, functional, and others). The build artifacts are reused in Docker &lt;strong&gt;build&lt;/strong&gt; to produce a new image. This becomes a core deployment artifact. The produced image contains not only application &amp;ldquo;binaries&amp;rdquo;, but also a required runtime including all dependencies and application configuration.&lt;/p&gt;
&lt;p&gt;We are getting application portability, however, we are loosing the development and testing portability. We&amp;rsquo;re not able to reproduce exactly the same development and testing environment outside the CI. To create a new test environment we&amp;rsquo;ll need to setup the testing tools (correct versions and plugins), configure runtime and OS settings, and get the same versions of test scripts as well as perhaps, the test data.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://alexei-led.github.io/img/naive.png&#34; alt=&#34;The Naive Testing Strategy&#34;&gt;&lt;/p&gt;
&lt;p&gt;To resolve these problems leads us to the next one.&lt;/p&gt;
&lt;h2 id=&#34;app--test-container-approach&#34;&gt;App &amp;amp; Test Container approach&lt;/h2&gt;
&lt;p&gt;Here, we try to create a single bundle with the application &amp;ldquo;binaries&amp;rdquo; including required packages, testing tools (specific versions), test tools plugins, test scripts, test environment with all required packages.&lt;/p&gt;
&lt;p&gt;The benefits of this approach:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;We have a repeatable test environment - we can run exactly the same tests using the same testing tools - in our CI, development, staging, or production environment&lt;/li&gt;
&lt;li&gt;We capture test scripts at a specific point in time so we can always reproduce them in any environment&lt;/li&gt;
&lt;li&gt;We do not need to setup and configure our testing tools - they are part of our image&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;This approach has significant drawbacks:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Increases the image size - because it contains testing tools, required packages, test scripts, and perhaps even test data&lt;/li&gt;
&lt;li&gt;Pollutes image runtime environment with test specific configuration and may even introduce an unneeded dependency (required by integration testing)&lt;/li&gt;
&lt;li&gt;We also need to decide what to do with the test results and logs; how and where to export them&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Here&amp;rsquo;s a simplified &lt;code&gt;Dockerfile&lt;/code&gt;. It illustrates this approach.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-dockerfile&#34; data-lang=&#34;dockerfile&#34;&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;FROM&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt; &amp;#34;&amp;lt;bases image&amp;gt;&amp;#34;:&amp;#34;&amp;lt;version&amp;gt;&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;WORKDIR&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt; &amp;#34;&amp;lt;path&amp;gt;&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# install packages required to run app and tests&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;RUN&lt;/span&gt; apt-get update &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; apt-get install -y &lt;span style=&#34;color:#ae81ff&#34;&gt;\
&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&amp;lt;app runtime&amp;gt; and &amp;lt;dependencies&amp;gt;&amp;#34;&lt;/span&gt; &lt;span style=&#34;color:#ae81ff&#34;&gt;\ &lt;/span&gt; &lt;span style=&#34;color:#75715e&#34;&gt;# add app runtime and required packages&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&amp;lt;test tools&amp;gt; and &amp;lt;dependencies&amp;gt;&amp;#34;&lt;/span&gt; &lt;span style=&#34;color:#ae81ff&#34;&gt;\ &lt;/span&gt;    &lt;span style=&#34;color:#75715e&#34;&gt;# add testing tools and required packages&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; rm -rf /var/lib/apt/lists/*&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# copy app files&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;COPY&lt;/span&gt; app app&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;COPY&lt;/span&gt; run.sh run.sh&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# copy test scripts&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;COPY&lt;/span&gt; tests tests&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# copy &amp;#34;main&amp;#34; test command&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;COPY&lt;/span&gt; test.sh test.sh&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# ... EXPOSE, RUN, ADD ... for app and test environment&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# main app command&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;CMD&lt;/span&gt; [&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;r&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;u&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;n&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;s&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;h&lt;/span&gt;, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&amp;lt;app arguments&amp;gt;&amp;#34;&lt;/span&gt;]&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# it&amp;#39;s not possible to have multiple CMD commands, but this is the &amp;#34;main&amp;#34; test command&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# CMD [/test.sh, &amp;#34;&amp;lt;test arguments&amp;gt;&amp;#34;]&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;&lt;a href=&#34;https://gist.github.com/alexei-led/cbb3d46fcf422a24aacd&#34;&gt;Dockerfile&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://alexei-led.github.io/img/app_test.png&#34; alt=&#34;App &amp; Test Container&#34;&gt;&lt;/p&gt;
&lt;p&gt;There has to be a better way for in-container testing and there is.&lt;/p&gt;
&lt;h2 id=&#34;test-aware-container-approach&#34;&gt;Test Aware Container Approach&lt;/h2&gt;
&lt;p&gt;Today, Docker&amp;rsquo;s promise is &lt;strong&gt;&amp;ldquo;Build -&amp;gt; Ship -&amp;gt; Run&amp;rdquo;&lt;/strong&gt; - build the image, ship it to some registry, and run it anywhere. IMHO there&amp;rsquo;s a critical missing step - &lt;strong&gt;Test&lt;/strong&gt;. The right and complete sequence should be &lt;strong&gt;:Build -&amp;gt; Test -&amp;gt; Ship -&amp;gt; Run&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;Let&amp;rsquo;s look at a &amp;ldquo;test-friendly&amp;rdquo; Dockerfile syntax and extensions to Docker commands. This important step could be supported natively. It&amp;rsquo;s not a real syntax, but bear with me. I&amp;rsquo;ll define the &amp;ldquo;ideal&amp;rdquo; version and show how to implement something that&amp;rsquo;s very close.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ONTEST [INSTRUCTION]
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Let&amp;rsquo;s define a special &lt;code&gt;ONTEST&lt;/code&gt; instruction, similar to existing &lt;a href=&#34;https://docs.docker.com/engine/reference/builder/#onbuild&#34;&gt;&lt;code&gt;ONBUILD&lt;/code&gt;&lt;/a&gt; instruction. The &lt;code&gt;ONTEST&lt;/code&gt; instruction adds a trigger instruction to the image to be executed at a later time when the image is tested. Any build instruction can be registered as a trigger.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;ONTEST&lt;/code&gt; instruction should be recognized by a new &lt;code&gt;docker test&lt;/code&gt; command.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;docker test [OPTIONS] IMAGE [COMMAND] [ARG...]
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;The &lt;code&gt;docker test&lt;/code&gt; command syntax will be similar to &lt;code&gt;docker run&lt;/code&gt; command, with one significant difference: a new &amp;ldquo;testable&amp;rdquo; image will be automatically generated and even tagged with &lt;code&gt;&amp;lt;image name&amp;gt;:&amp;lt;image tag&amp;gt;-test&lt;/code&gt; tag (&amp;ldquo;test&amp;rdquo; postfix added to the original image tag). This &amp;ldquo;testable&amp;rdquo; image will generated &lt;code&gt;FROM&lt;/code&gt; the application image, executing all build instructions, defined after &lt;code&gt;ONTEST&lt;/code&gt; command and executing &lt;code&gt;ONTEST CMD&lt;/code&gt; (or &lt;code&gt;ONTEST ENTRYPOINT&lt;/code&gt;).
The &lt;code&gt;docker test&lt;/code&gt; command should return a non-zero code if any tests fail. The test results should be written into an automatically generated &lt;code&gt;VOLUME&lt;/code&gt; that points to &lt;code&gt;/var/tests/results&lt;/code&gt; folder.&lt;/p&gt;
&lt;p&gt;Let&amp;rsquo;s look at a modified &lt;code&gt;Dockerfile&lt;/code&gt; below - it includes the new proposed &lt;code&gt;ONTEST&lt;/code&gt; instruction.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-dockerfile&#34; data-lang=&#34;dockerfile&#34;&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;FROM&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt; &amp;#34;&amp;lt;base image&amp;gt;&amp;#34;:&amp;#34;&amp;lt;version&amp;gt;&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;WORKDIR&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt; &amp;#34;&amp;lt;path&amp;gt;&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# install packages required to run app&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;RUN&lt;/span&gt; apt-get update &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; apt-get install -y &lt;span style=&#34;color:#ae81ff&#34;&gt;\
&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&amp;lt;app runtime&amp;gt; and &amp;lt;dependencies&amp;gt;&amp;#34;&lt;/span&gt; &lt;span style=&#34;color:#ae81ff&#34;&gt;\ &lt;/span&gt; &lt;span style=&#34;color:#75715e&#34;&gt;# add app runtime and required packages&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;    &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; rm -rf /var/lib/apt/lists/*&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# install packages required to run tests   &lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;ONTEST RUN apt-get update &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; apt-get install -y &lt;span style=&#34;color:#ae81ff&#34;&gt;\
&lt;/span&gt;&lt;span style=&#34;color:#ae81ff&#34;&gt;&lt;/span&gt;           &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&amp;lt;test tools&amp;gt; and &amp;lt;dependencies&amp;gt;&amp;#34;&lt;/span&gt;    &lt;span style=&#34;color:#ae81ff&#34;&gt;\ &lt;/span&gt;    &lt;span style=&#34;color:#75715e&#34;&gt;# add testing tools and required packages&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;           &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; rm -rf /var/lib/apt/lists/*&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# copy app files&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;COPY&lt;/span&gt; app app&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;COPY&lt;/span&gt; run.sh run.sh&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# copy test scripts&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;ONTEST COPY tests tests&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# copy &amp;#34;main&amp;#34; test command&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;ONTEST COPY test.sh test.sh&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# auto-generated volume for test results&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# ONTEST VOLUME &amp;#34;/var/tests/results&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# ... EXPOSE, RUN, ADD ... for app and test environment&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# main app command&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#66d9ef&#34;&gt;CMD&lt;/span&gt; [&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;r&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;u&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;n&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;.&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;s&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;h&lt;/span&gt;, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&amp;lt;app arguments&amp;gt;&amp;#34;&lt;/span&gt;]&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;# main test command&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;&lt;/span&gt;ONTEST CMD &lt;span style=&#34;color:#f92672&#34;&gt;[&lt;/span&gt;/test.sh, &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&amp;lt;test arguments&amp;gt;&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#f92672&#34;&gt;]&lt;/span&gt;&lt;span style=&#34;color:#960050;background-color:#1e0010&#34;&gt;
&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;&lt;a href=&#34;https://gist.github.com/alexei-led/4f39cca8a03b1503978a&#34;&gt;Dockerfile&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://alexei-led.github.io/img/test_aware.png&#34; alt=&#34;Test Aware Container&#34;&gt;&lt;/p&gt;
&lt;h3 id=&#34;making-test-aware-container-real&#34;&gt;Making &amp;ldquo;Test Aware Container&amp;rdquo; Real&lt;/h3&gt;
&lt;p&gt;We believe Docker should make &lt;code&gt;docker-test&lt;/code&gt; part of the container management lifecycle. There is a need to have a simple working solution today and I&amp;rsquo;ll describe one that&amp;rsquo;s very close to the ideal state.&lt;/p&gt;
&lt;p&gt;As mentioned before, Docker has a very useful &lt;code&gt;ONBUILD&lt;/code&gt; instruction. This instruction allows us to trigger another build instruction on succeeding builds. The basic idea is to use &lt;code&gt;ONBUILD&lt;/code&gt; instruction when running &lt;code&gt;docker-test&lt;/code&gt; command.&lt;/p&gt;
&lt;p&gt;The flow executed by &lt;code&gt;docker-test&lt;/code&gt; command:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;docker-test&lt;/code&gt; will search for &lt;code&gt;ONBUILD&lt;/code&gt; instructions in application &lt;code&gt;Dockerfile&lt;/code&gt; and will &amp;hellip;&lt;/li&gt;
&lt;li&gt;generate a temporary &lt;code&gt;Dockerfile.test&lt;/code&gt; from original &lt;code&gt;Dockerfile&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;execute &lt;code&gt;docker build -f Dockerfile.test [OPTIONS] PATH&lt;/code&gt; with additional options supported by &lt;code&gt;docker build&lt;/code&gt; command: &lt;code&gt;-test&lt;/code&gt; that will be automatically appended to &lt;code&gt;tag&lt;/code&gt; option&lt;/li&gt;
&lt;li&gt;If build is successful, execute &lt;code&gt;docker run -v ./tests/results:/var/tests/results [OPTIONS] IMAGE:TAG-test [COMMAND] [ARG...]&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;Remove &lt;code&gt;Dockerfile.test&lt;/code&gt; file&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Why not create a new &lt;code&gt;Dockerfile.test&lt;/code&gt; without requiring the &lt;code&gt;ONBUILD&lt;/code&gt; instruction?&lt;/p&gt;
&lt;p&gt;Because in order to test right image (and tag) we&amp;rsquo;ll need to keep &lt;code&gt;FROM&lt;/code&gt; always updated to &lt;strong&gt;image:tag&lt;/strong&gt; that we want to test. This is not trivial.&lt;/p&gt;
&lt;p&gt;There is a limitation in the described approach - it&amp;rsquo;s not suitable for &amp;ldquo;onbuild&amp;rdquo; images (images used to automatically build your app), like &lt;a href=&#34;https://hub.docker.com/_/maven/&#34;&gt;Maven:onbuild&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Let&amp;rsquo;s look at a simple implementation of &lt;code&gt;docker-test&lt;/code&gt; command. It highlights the concept: the &lt;code&gt;docker-test&lt;/code&gt; command should be able to handle &lt;code&gt;build&lt;/code&gt; and &lt;code&gt;run&lt;/code&gt; command options and be able to handle errors properly.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;&lt;span style=&#34;color:#75715e&#34;&gt;#!/bin/bash
&lt;/span&gt;&lt;span style=&#34;color:#75715e&#34;&gt;&lt;/span&gt;image&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;app&amp;#34;&lt;/span&gt;
tag&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;latest&amp;#34;&lt;/span&gt;

echo &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;FROM &lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;${&lt;/span&gt;image&lt;span style=&#34;color:#e6db74&#34;&gt;}&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;:&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;${&lt;/span&gt;tag&lt;span style=&#34;color:#e6db74&#34;&gt;}&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt; &amp;gt; Dockerfile.test &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt;
docker build -t &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;${&lt;/span&gt;image&lt;span style=&#34;color:#e6db74&#34;&gt;}&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;:&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;${&lt;/span&gt;tag&lt;span style=&#34;color:#e6db74&#34;&gt;}&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;-test&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt; -f Dockerfile.test . &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt;
docker run -it --rm -v &lt;span style=&#34;color:#66d9ef&#34;&gt;$(&lt;/span&gt;pwd&lt;span style=&#34;color:#66d9ef&#34;&gt;)&lt;/span&gt;/tests/results:/var/tests/results &lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;${&lt;/span&gt;image&lt;span style=&#34;color:#e6db74&#34;&gt;}&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;:&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;${&lt;/span&gt;tag&lt;span style=&#34;color:#e6db74&#34;&gt;}&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;-test&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;&lt;/span&gt; &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt;
rm Dockerfile.test
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Let&amp;rsquo;s focus on the most interesting and relevant part.&lt;/p&gt;
&lt;h2 id=&#34;integration-test-container&#34;&gt;Integration Test Container&lt;/h2&gt;
&lt;p&gt;Let&amp;rsquo;s say we have an application built from tens or hundreds of microservices. Let&amp;rsquo;s say we have an automated CI/CD pipeline, where each microservice is built and tested by our CI and deployed into some environment (testing, staging or production) after the build and tests pass. Pretty cool, eh?
Our CI tests are capable of testing each microservice in isolation - running unit and service tests (or API contract tests). Maybe even micro-integration tests - tests run on subsystem are created in ad-hoc manner (for example with &lt;code&gt;docker compose&lt;/code&gt; help).&lt;/p&gt;
&lt;p&gt;This leads to some issues that we need to address:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;What about real integration tests or long running tests (like performance and stress)?&lt;/li&gt;
&lt;li&gt;What about resilience tests (&amp;ldquo;chaos monkey&amp;rdquo; like tests)?&lt;/li&gt;
&lt;li&gt;Security scans?&lt;/li&gt;
&lt;li&gt;What about test and scan activities that take time and should be run on a fully operational system?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;There should be a better way than just dropping a new microservice version into production and tightly monitoring it for a while.&lt;/p&gt;
&lt;p&gt;There should be a special &lt;strong&gt;Integration Test Container&lt;/strong&gt;. These containers will contain only testing tools and test artifacts: test scripts, test data, test environment configuration, etc. To simplify orchestration and automation of such containers, we should define and follow some conventions and use metadata labels (Dockerfile &lt;code&gt;LABEL&lt;/code&gt; instruction).&lt;/p&gt;
&lt;h3 id=&#34;integration-test-labels&#34;&gt;Integration Test Labels&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;test.type&lt;/strong&gt; - test type; default &lt;code&gt;integration&lt;/code&gt;; can be one of: &lt;code&gt;integration&lt;/code&gt;, &lt;code&gt;performance&lt;/code&gt;, &lt;code&gt;security&lt;/code&gt;, &lt;code&gt;chaos&lt;/code&gt; or any text; presence of this label states that this is an &lt;strong&gt;Integration Test Container&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;test.results&lt;/strong&gt; - &lt;code&gt;VOLUME&lt;/code&gt; for test results; default &lt;code&gt;/var/tests/results&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;test.XXX&lt;/strong&gt; - any other test related metadata; just use &lt;strong&gt;test.&lt;/strong&gt; prefix for label name&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&#34;integration-test-container-1&#34;&gt;Integration Test Container&lt;/h3&gt;
&lt;p&gt;The &lt;strong&gt;Integration Test Container&lt;/strong&gt; is just a regular Docker container. Tt does not contain any application logic and code. Its sole purpose is to create repeatable and portable testing. Recommended content of the &lt;strong&gt;Integration Test Container&lt;/strong&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;em&gt;The Testing Tool&lt;/em&gt; - Phantom.js, Selenium, Chakram, Gatling, &amp;hellip;&lt;/li&gt;
&lt;li&gt;&lt;em&gt;Testing Tool Runtime&lt;/em&gt; - Node.js, JVM, Python, Ruby, &amp;hellip;&lt;/li&gt;
&lt;li&gt;&lt;em&gt;Test Environment Configuration&lt;/em&gt; - environment variables, config files, bootstrap scripts, &amp;hellip;&lt;/li&gt;
&lt;li&gt;&lt;em&gt;Tests&lt;/em&gt; - as compiled packages or script files&lt;/li&gt;
&lt;li&gt;&lt;em&gt;Test Data&lt;/em&gt; - any kind of data files, used by tests: json, csv, txt, xml, &amp;hellip;&lt;/li&gt;
&lt;li&gt;&lt;em&gt;Test Startup Script&lt;/em&gt; - some &amp;ldquo;main&amp;rdquo; startup script to run tests; just create &lt;code&gt;test.sh&lt;/code&gt; and launch the testing tool from it.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;Integration Test Containers&lt;/strong&gt; should run in an operational environment where all microservices are deployed: testing, staging or production. These containers can be deployed exactly as all other services. They use same network layer and thus can access multiple services; using selected service discovery method (usually DNS). Accessing multiple services is required for real integration testing - we need to simulate and validate how our system is working in multiple places. Keeping integration tests inside some application service container not only increases the container footprint but also creates an unneeded dependency between multiple services. We keep all these dependencies at the level of the &lt;strong&gt;Integration Test Container&lt;/strong&gt;. Once our tests (and testing tools) are packaged inside the container, we can always rerun the same tests on any environment including the developer machine. You can always go back in time and rerun a specific version of &lt;strong&gt;Integration Test Container&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;&lt;img src=&#34;https://alexei-led.github.io/img/int_test.png&#34; alt=&#34;Integration Test Container&#34;&gt;&lt;/p&gt;
&lt;p&gt;WDYT? Your feedback, particularly on standardizing the &lt;code&gt;docker-test&lt;/code&gt; command, is greatly appreciated.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Docker Pattern: The Build Container</title>
      <link>https://alexei-led.github.io/post/docker_builder_pattern/</link>
      <pubDate>Thu, 14 Jan 2016 16:14:47 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/docker_builder_pattern/</guid>
      <description>&lt;p&gt;Let&amp;rsquo;s say that you&amp;rsquo;re developing a microservice in a compiled language or an interpreted language that requires some additional “build” steps to package and lint your application code. This is a useful docker pattern for the &amp;ldquo;build&amp;rdquo; container.&lt;/p&gt;
&lt;p&gt;In our project, we&amp;rsquo;re using Docker as our main deployment package: every microservice is delivered as a Docker image. Each microservice also has it’s own code repository (GitHub), and its own CI build job.&lt;/p&gt;
&lt;p&gt;Some of our services are written in Java. Java code requires additional tooling and processes before you get working code. This tooling and all associated packages are depended from and not required when a compiled program is running.&lt;/p&gt;
&lt;p&gt;We have been trying to develop a repeatable process and uniform environment for deploying our services. We believe it’s good to package Java tools and packages into containers too. It allows us to build a Java-based microservice on any machine, including CI, without any specific environment requirements: JDK version, profiling and testing tools, OS, Maven, environment variables, etc.&lt;/p&gt;
&lt;p&gt;For every service, we have two(2) Dockerfiles: one for service runtime and the second packed with tools required to build the service. We usually name these files as &lt;code&gt;Dockerfile&lt;/code&gt; and &lt;code&gt;Dockerfile.build&lt;/code&gt;. We are using &lt;code&gt;-f, --file=&amp;quot;&amp;quot;&lt;/code&gt; flag to specify Dockerfile file name for &lt;code&gt;docker build&lt;/code&gt; command.&lt;/p&gt;
&lt;p&gt;Here is our &lt;code&gt;Dockerfile.build&lt;/code&gt; file:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;FROM maven:3.3.3-jdk-8

ENV GAIA_HOME&lt;span style=&#34;color:#f92672&#34;&gt;=&lt;/span&gt;/usr/local/gaia/

RUN mkdir -p $GAIA_HOME
WORKDIR $GAIA_HOME

&lt;span style=&#34;color:#75715e&#34;&gt;# speed up maven build, read https://keyholesoftware.com/2015/01/05/caching-for-maven-docker-builds/&lt;/span&gt;
&lt;span style=&#34;color:#75715e&#34;&gt;# selectively add the POM file&lt;/span&gt;
ADD pom.xml $GAIA_HOME

&lt;span style=&#34;color:#75715e&#34;&gt;# get all the downloads out of the way&lt;/span&gt;
RUN &lt;span style=&#34;color:#f92672&#34;&gt;[&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;mvn&amp;#34;&lt;/span&gt;,&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;verify&amp;#34;&lt;/span&gt;,&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;clean&amp;#34;&lt;/span&gt;,&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;--fail-never&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#f92672&#34;&gt;]&lt;/span&gt;

&lt;span style=&#34;color:#75715e&#34;&gt;# add source&lt;/span&gt;
ADD . $GAIA_HOME

&lt;span style=&#34;color:#75715e&#34;&gt;# run maven verify&lt;/span&gt;
RUN &lt;span style=&#34;color:#f92672&#34;&gt;[&lt;/span&gt;&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;mvn&amp;#34;&lt;/span&gt;,&lt;span style=&#34;color:#e6db74&#34;&gt;&amp;#34;verify&amp;#34;&lt;/span&gt;&lt;span style=&#34;color:#f92672&#34;&gt;]&lt;/span&gt;
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;As you can see, it’s a simple file with one little trick to speed up our Maven build.&lt;/p&gt;
&lt;p&gt;Now, we have all the tools required to compile our service. We can run this Docker container on any machine without requiring to have JDK installed. We can run the same Docker container on a developer&amp;rsquo;s laptop and on our CI server.&lt;/p&gt;
&lt;p&gt;This trick greatly simplifies our CI process - we no longer require our CI to support any specific compiler, version, or tool. All we need is the Docker engine. Everything else, we bring ourselves.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;BYOT - Bring Your Own Tooling! :-)&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;In order to compile the service code, we need to build and run the &lt;strong&gt;builder&lt;/strong&gt; container.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;docker build -t build-img -f Dockerfile.build
docker create --name build-cont build-img
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Once we’ve built the image and created a new container from this image, we have our service compiled inside the container. The only remaining task is to extract build artifacts from the container. We could use Docker volumes - this is one possible option. Actually, we like that the image we’ve created, contains all the tools and build artifacts inside it. It allows us to get any build artifacts from this image at anytime, just by creating a new container from it.&lt;/p&gt;
&lt;p&gt;To extract our build artifacts, we are using &lt;code&gt;docker cp&lt;/code&gt; command. This command copies files from the container to the local file system.&lt;/p&gt;
&lt;p&gt;Here is how we are using this command:&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;docker cp build-cont:/usr/local/gaia/target/mgs.war ./target/mgs.war
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;As a result, we have a compiled service code, packaged into single WAR file. We get exactly the same WAR file on any machine just by running our &lt;strong&gt;builder&lt;/strong&gt; container, or by rebuilding the &lt;strong&gt;builder&lt;/strong&gt; container against the same code commit (using Git tag or specific commit ID) on any machine.&lt;/p&gt;
&lt;p&gt;We can now create a Docker image with our service and required runtime, which is usually some version of JRE and servlet container.&lt;/p&gt;
&lt;p&gt;Here is our &lt;code&gt;Dockerfile&lt;/code&gt; for the service. It&amp;rsquo;s an image with Jetty JRE8 and our service WAR file.&lt;/p&gt;
&lt;div class=&#34;highlight&#34;&gt;&lt;pre style=&#34;color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4&#34;&gt;&lt;code class=&#34;language-sh&#34; data-lang=&#34;sh&#34;&gt;FROM jetty:9.3.0-jre8

RUN mkdir -p /home/jetty &lt;span style=&#34;color:#f92672&#34;&gt;&amp;amp;&amp;amp;&lt;/span&gt; chown jetty:jetty /home/jetty

COPY ./target/*.war $JETTY_BASE/webapps/
&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;By running &lt;code&gt;docker build .&lt;/code&gt;, we have a new image with our newly “built” service.&lt;/p&gt;
&lt;h2 id=&#34;the-recipe&#34;&gt;The Recipe:&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;Have one &lt;code&gt;Dockerfile&lt;/code&gt; with all tools and packages required to build your service. Name it &lt;code&gt;Dockerfile.build&lt;/code&gt; or give it a name you like.&lt;/li&gt;
&lt;li&gt;Have another &lt;code&gt;Dockerfile&lt;/code&gt; with all packages required to run your service.&lt;/li&gt;
&lt;li&gt;Keep both files with your service code.&lt;/li&gt;
&lt;li&gt;Build a new &lt;strong&gt;builder&lt;/strong&gt; image, create a container from it and extract build artifacts, using volumes or &lt;code&gt;docker cp&lt;/code&gt; command.&lt;/li&gt;
&lt;li&gt;Build the service image.&lt;/li&gt;
&lt;li&gt;That&amp;rsquo;s all folks!&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&#34;summary&#34;&gt;Summary&lt;/h2&gt;
&lt;p&gt;In our case, Java-based service, the difference between &lt;strong&gt;builder&lt;/strong&gt; container and service container is huge. Java JDK is much bigger package than JRE: it also requires all X Window packages installed inside your container. For the runtime, you can have a slim image with your service code, JRE, and some basic Linux packages, or even start from &lt;code&gt;scratch&lt;/code&gt;.&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>Docker Pattern: Deploy and update dockerized application on cluster</title>
      <link>https://alexei-led.github.io/post/docker_cd_cluster/</link>
      <pubDate>Wed, 30 Dec 2015 11:09:47 +0200</pubDate>
      <guid>https://alexei-led.github.io/post/docker_cd_cluster/</guid>
      <description>&lt;p&gt;Docker is great technology that simplifies development and deployment of distributed applications.&lt;/p&gt;
&lt;p&gt;While Docker serves as a core technology, many issues remain to be solved. We find ourselves struggling with some of these issues. For example:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;How to create an elastic Docker cluster?&lt;/li&gt;
&lt;li&gt;How to deploy and connect multiple containers together?&lt;/li&gt;
&lt;li&gt;How to build CI/CD process?&lt;/li&gt;
&lt;li&gt;How to register and discover system services and more?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;For most, there are open source projects, or services available from the community as well as commercially, including from Docker, Inc.&lt;/p&gt;
&lt;p&gt;In this post, I would like to address one such problem:&lt;/p&gt;
&lt;h3 id=&#34;how-to-automate-continuous-delivery-for-a-docker-cluster&#34;&gt;How to automate Continuous Delivery for a Docker cluster?&lt;/h3&gt;
&lt;p&gt;In our application, every service is packaged as a Docker image. Docker image is our basic deployment unit. In a runtime environment (production, testing and others), we may have multiple containers, created from this image. For each service, we have a separate Git repository and one or more Dockerfile(s). We use this for building, testing, and packaging - will explain our project structure in the next blog post.&lt;/p&gt;
&lt;p&gt;We&amp;rsquo;ve setup an automated Continuous Integration(CI) process. We use CircleCI. For every push to some branch for the service in a Git repository, the CI service triggers a new Docker build process and creates a new Docker image for the service. As part of the build process, if everything compiles and all unit and component tests pass, we push a newly created Docker image to our DockerHub repository.&lt;/p&gt;
&lt;p&gt;At the end of CI process, we have a newly tested and ‘kosher’ Docker image in our DockerHub repository. Very nice indeed! However, we are left with several questions:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;How to perform a rolling update of a modified service?&lt;/li&gt;
&lt;li&gt;What is the target environment (i.e. some Docker cluster)?&lt;/li&gt;
&lt;li&gt;How to find the ip of a dynamically created CoreOS host (we use AWS auto-scale groups)?&lt;/li&gt;
&lt;li&gt;How to connect to it in a secure way without the need to expose the SSH keys or cloud credentials (we use AWS for infrastructure)?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Our application runs on a CoreOS cluster. We have automation scripts that creates the CoreOS cluster on multiple environments: developer machine, AWS, or some VM infrustructure. When we have a new service version (i.e. Docker image), we need to find a way to deploy this service to the cluster. CoreOS uses &lt;strong&gt;fleet&lt;/strong&gt; for cluster orchestration. &lt;strong&gt;Fleetctl&lt;/strong&gt; is a command line client that can talk with the &lt;strong&gt;fleet&lt;/strong&gt; backend and allows you to manage the CoreOS cluster. However, this only works in a local environment (machine or network). For some commands, &lt;strong&gt;fleetctl&lt;/strong&gt; uses HTTP API and for others an SSH connection. The HTTP API is not protected so it makes no sense to expose it from your cloud environment. The SSH connection does not work when your architecture assumes there is no direct SSH connection to the cluster. Connecting through some SSH bastion machine, requiring the use of multiple SSH keys, and creating SSH configuration files, are not supported by the &lt;strong&gt;fleetctl&lt;/strong&gt; program. I have no desire to store SSH keys or my cloud credentials for my production environment on any CI server due to security concerns.&lt;/p&gt;
&lt;h3 id=&#34;so-what-do-we-do&#34;&gt;So, what do we do?&lt;/h3&gt;
&lt;p&gt;First, we want to deploy a new service or update some service when there is a new Docker image or image version available. We also want to be selective and pick images created from code in a specific branch or even specific build.&lt;/p&gt;
&lt;p&gt;The basic idea is to create a Docker image that captures the system configuration. This image stores the configuration of all services at a specific point in time and from specific branches. The container created from this image should be run from within the target cluster. Besides captured configuration, we also have the deployment tool (&lt;strong&gt;fleetctl&lt;/strong&gt; in our case), plus some code that detects services which need to be updated, deleted, or installed as a new service.&lt;/p&gt;
&lt;p&gt;This idea leads to another question:&lt;/p&gt;
&lt;h3 id=&#34;how-do-you-define-and-capture-system-configuration&#34;&gt;How do you define and capture system configuration?&lt;/h3&gt;
&lt;p&gt;In CoreOS, every service can be described in &lt;strong&gt;systemd unit&lt;/strong&gt; files. This is a plain text file that describes how and when to launch your service. I’m not going to explain how to write such files. There is a lot of info online. What&amp;rsquo;s important in our case, the service systemd unit file contains a &lt;code&gt;docker run&lt;/code&gt; command with parameters and image name that needs to be downloaded and executed. We keep the service systemd unit file in the same repository as the service code.&lt;/p&gt;
&lt;p&gt;The image name: usually defined as a &lt;code&gt;repository/image_name:tag&lt;/code&gt; string. &lt;strong&gt;Tag&lt;/strong&gt; is the most important thing in our solution. As explained above, our CI server automatically builds a new Docker image on every push of the service to the GitHub repository. The CI job also tags the newly created image with 2 tags:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;branch&lt;/code&gt; tag — taken from GitHub branch that triggered the build (&lt;code&gt;master&lt;/code&gt;, &lt;code&gt;develop&lt;/code&gt;, &lt;code&gt;feature-*&lt;/code&gt; braches in our case)&lt;/li&gt;
&lt;li&gt;&lt;code&gt;build_num-branch&lt;/code&gt; tag — where we add a running build number prefix, just before branch name
As a result, in DockerHub, we have images for the latest build in any branch and also for every image we can identify the build job number and the branch it was created from.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;As I said before, we keep service systemd unit file in the same repository as code. This file &lt;strong&gt;does not&lt;/strong&gt; contain an image tag, only the repository and image name. See example below:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ExecStart=/usr/bin/docker run —name myservice -p 3000:3000 myrepo/myservice
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Our CI service build job generates a new service &lt;strong&gt;systemd&lt;/strong&gt; unit file for every successful build, replacing the above service invocation command with one that also contains a new tag. Using &lt;code&gt;build_num-branch&lt;/code&gt; pattern (&lt;code&gt;develop&lt;/code&gt; branch in our example), we use two simple utilities for this job: &lt;code&gt;cat&lt;/code&gt; and &lt;code&gt;sed&lt;/code&gt;. It’s possible to use a more advanced template engine.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;ExecStart=/usr/bin/docker run — name myservice -p 3000:3000 myrepo/myservice:58-develop
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;As a last step, the CI build job “deploys” this new unit file to the system configuration Git repository.&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;git commit -am “Updating myservice tag to ‘58-develop’” myservice.service
git push origin develop
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Another CI job that monitors changes in the system configuration repository will trigger a build and create a new Docker image that captures updated system configuration.&lt;/p&gt;
&lt;p&gt;All we need to do now is to execute this Docker image on the Docker cluster. Something like this:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;docker run -it —-rm=true myrepo/mysysconfig:develop
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;Our &lt;strong&gt;system configuration&lt;/strong&gt; Dockerfile:&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;FROM alpine:3.2
MAINTAINER Alexei Ledenev &amp;lt;alexei.led@gmail.com&amp;gt;

ENV FLEET_VERSION 0.11.5

ENV gaia /home/gaia
RUN mkdir -p ${gaia}

WORKDIR ${gaia}
COPY *.service ./
COPY deploy.sh ./
RUN chmod +x deploy.sh

# install packages and fleetctl client
RUN apk update &amp;amp;&amp;amp; \
 apk add curl openssl bash &amp;amp;&amp;amp; \
 rm -rf /var/cache/apk/* &amp;amp;&amp;amp; \
 curl -L “https://github.com/coreos/fleet/releases/download/v${FLEET_VERSION}/fleet-v${FLEET_VERSION}-linux-amd64.tar.gz&amp;quot; | tar xz &amp;amp;&amp;amp; \
 mv fleet-v${FLEET_VERSION}-linux-amd64/fleetctl /usr/local/bin/ &amp;amp;&amp;amp; \
 rm -rf fleet-v${FLEET_VERSION}-linux-amd64

CMD [“/bin/bash”, “deploy.sh”]
&lt;/code&gt;&lt;/pre&gt;&lt;p&gt;&lt;code&gt;deploy.sh&lt;/code&gt; is a shell script for every service check. If it needs to be updated, created, or deleted, it executes the corresponding &lt;strong&gt;fleetctl&lt;/strong&gt; command.&lt;/p&gt;
&lt;p&gt;The final step:&lt;/p&gt;
&lt;h3 id=&#34;how-do-you-run-this-system-configuration-container&#34;&gt;How do you run this “system configuration” container?&lt;/h3&gt;
&lt;p&gt;Currently, in our environment, we do this manually (from SSH shell) for development clusters and use systemd timers for CoreOS clusters on AWS. Systemd timer allows us to define a cron like job at the CoreOS cluster level.&lt;/p&gt;
&lt;p&gt;We have plans to define a WebHook endpoint that will allow us to trigger deployment/update based on a WebHook event from the CI service.&lt;/p&gt;
&lt;p&gt;Hope you find this post useful. I look forward to your comments and any questions you have.&lt;/p&gt;
</description>
    </item>
    
  </channel>
</rss>
